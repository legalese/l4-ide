# P2a — the picture baseline: `bpmn-js-token-simulation` over P1's shipped BPMN

**Status: MEASURED 2026-09-15 over eight goldens; RE-MEASURED 2026-09-15 17:19 UTC over all fourteen (2026-09-16 SGT); RE-MEASURED again 2026-09-15 20:25 UTC over all fourteen, after `d544ed22` moved `modals-may-fork`; RUN 4 2026-09-18 23:23 UTC over all sixteen (2026-09-19 SGT), the first diffable baseline** (the
six `modals-*` goldens `6daf1d9d` added on legalese/l4-ide#395 are covered in §3.7; the five fork
goldens are now multi-instance sub-processes, §3.1 and §3.7; the committed `out/` is run 4 in
full — one run, all sixteen files from it, provenance generated by `run.mjs` — see the **Run 4**
bullet below and `out/run-meta.json`). This is the experiment [LTS-VISUALISER.md](./LTS-VISUALISER.md)
§7.2 stages as **P2a** — _"point `bpmn-io/bpmn-js-token-simulation` (MIT) at P1's shipped output
and write down, case by case, what it cannot say."_ It is **not** the gate. §7.3 makes P2a′ (the
list baseline, a reader experiment) the primary gate and this the secondary one, and §7.2's second
table says what P2a cannot falsify; the last section here restates that against what was seen and
decides nothing.

What was run, on what, with what:

- **Apparatus:** [`etc/bpmn-token-sim/`](../../../etc/bpmn-token-sim/README.md) — a self-contained
  harness (own `package.json`; the root and `ts-apps/` lockfiles are untouched) that bundles
  `bpmn-js` with the token-simulation module, opens each fixture headlessly in Google Chrome via
  Playwright, plays three scenarios, screenshots each and dumps everything the simulator's own
  services report to `out/<fixture>.json`.
- **Input:** the sixteen committed goldens in `jl4/examples/bpmn/expected/*.bpmn` at this branch
  (`ls | wc -l`: 16) — the eight of the first run, `consultation`, `handover`, `offering`,
  `regcf-advertising`, `regcf-reporting`, `regcf-resale`, `tenancy-barrier`, `tenancy-fork`; the
  six from `jl4/examples/bpmn/modals.l4` that `6daf1d9d` (legalese/l4-ide#395) added after that
  run: `modals-may-barrier`, `modals-may-fork`, `modals-shant-barrier`, `modals-shant-fork`,
  `modals-must-barrier-both-deadlines`, `modals-must-fork-join-deadline`; `option`
  (`jl4/examples/bpmn/option.l4`, legalese/l4-ide#425, 2026-09-17 — a single `PARTY … MAY` whose
  `HENCE` is another party's duty); and `tenancy-fork-beside-party` (`receipts and delivery` in
  `tenancy.l4`, legalese/l4-ide#430 — a fork `RAND` a `PARTY` obligation), the newcomer of run 4.
  No Haskell was built; the fixtures are P1's output as committed. Since #430 the five fork
  goldens (`tenancy-fork`, `tenancy-fork-beside-party`, `modals-may-fork`, `modals-shant-fork`,
  `modals-must-fork-join-deadline`) are multi-instance **sub-processes**, not multi-instance
  tasks; what the exporter claims for that shape lives in #430, and this document says only what
  the simulator shows of it.
- **Versions** (`etc/bpmn-token-sim/out/run-meta.json`, run 4, from `npm ls --depth=0`):

  ```
  bpmn-token-sim@0.1.0
  ├── bpmn-js-token-simulation@0.40.0
  ├── bpmn-js@18.28.0
  ├── esbuild@0.25.9
  ├── playwright@1.63.0
  └── randomcolor@0.6.2
  browser: chrome 153.0.8010.53 (Playwright channel 'chrome', headless)   node: v26.4.0
  ```

  (Runs 1–3 were on Chrome 152.0.7977.83 with no `randomcolor`; the seeded scope palette that
  needs it arrived with the determinism work, below.)

- **Run:** first, `cd etc/bpmn-token-sim && npm install && npm run build && npm run run` (the
  README now says `npm ci`; the lockfile that run produced is the one committed), 2026-09-15
  08:45 UTC, over the eight. Second, `npm ci && npm run build && npm run run`, 2026-09-15
  **17:19:57 UTC** (`out/run-meta.json`; 2026-09-16 in Singapore), over all fourteen, same
  Chrome, same Node, same `npm ls`. **The committed `out/` is the second run in full — the
  first run's eight JSONs and 28 screenshots were replaced, not kept**, for two reasons, each
  measured:

  1. The census gained a field (`multiInstance.completionCondition`, `src/app.js`, added for
     §3.7), so no first-run JSON is byte-comparable to a second-run one even apart from ids.
  2. With that field masked and the instance ids masked (`sed -E 's/ [0-9a-z]{6,7}"(,?)$/ <id>"\1/'`
     on both files), six of the eight were **identical** and two were not: `consultation` and
     `offering` differed in the **order** in which concurrent tokens appear in `history` and
     `tokensOn` after a parallel split (`Task_4` before `Task_1` on `consultation`; `Task_6`
     before `Task_7` on `offering`). A third run (17:13 UTC, discarded) showed the same kind of
     reordering on `handover` and `offering` and none on `consultation`. So the cross-run claim is
     now: runs differ in instance ids **and in the arrival order of concurrent tokens after a
     parallel split** — the simulator's animation timing, not its routing: the sets of tokens,
     the scopes, the triggers and the end events were the same in all three runs. Every
     `MEASURED` line in §3.1–§3.6 was re-read against the second run's JSON and still holds.
     Screenshots: 46, 28–58 KB each (`ls -la out/*.png`); none needed downscaling.

  Third, `npm ci && npm run build && npm run run`, 2026-09-15 **20:25:02 UTC** (`out/run-meta.json`;
  2026-09-16 04:25 in Singapore), over all fourteen, same Chrome, same Node, same `npm ls`, after
  `d544ed22` (legalese/l4-ide#395) replaced the `modals-may-fork` golden — its lapse timer is now
  `Boundary_0 → End_2`, not `Lapse_0 → Task_1`. **The committed `out/` WAS the third run in full;
  SUPERSEDED 2026-09-17, and again by run 4 below.** `handover` and the new `option` fixture were
  re-run on 2026-09-17 (Chrome 153) after legalese/l4-ide#425's PARTY MAY lapse arm turned
  `handover`'s four synthesised `Lapse_n` boundary events into ordinary `Boundary_n` ones; the
  other thirteen stayed the 2026-09-15 20:25:02 UTC bytes. A full re-run was deliberately NOT
  taken then, for the reason this very paragraph documents two sentences down: the instance ids
  and the parallel arrival order churned between runs, so re-running everything moved every file
  and hid the ones that actually changed. `out/run-meta.json` recorded provenance per fixture by
  hand as the honest substitute until the harness sorted `tokensOn`/`triggers` and masked the
  instance id — which it now does, and run 4 is that full re-run.
  With the instance ids masked (`jq 'del(.. | .log?)'` on both), twelve of the fourteen JSONs were
  **identical** to the second run's; `modals-may-fork` differs in the timer's id, its target and
  the whole `breach` scenario (§3.7), and `consultation` differs only in the arrival order of
  `Task_1`/`Task_4` after its parallel split — the same reordering the second run had already
  seen. Screenshots: 46, 28–58 KB each; none over 200 KB.

- **Diffable as of 2026-09-19 (harness commit `983d4a82a`, branch `lts/token-sim-determinism`).**
  The churn the three runs above kept working around — instance ids in every log line, and the
  arrival order of concurrent tokens in `tokensOn`/`triggers`/`history` — is gone from the
  harness, not from the report: `run.mjs` sorts every set-valued id array and masks scope ids to
  `<scope-N>` by first appearance per fixture; `src/app.js` replaces the simulator's token
  animation with one that completes in release order (the `history` reordering was a
  `requestAnimationFrame` race, not the routing — and not the library's `randomize` option
  either, which reads a field it never sets) and seeds the per-page scope palette; and
  `run-meta.json` carries provenance per fixture (`perFixture`, merged across subset runs). The
  acceptance test, run on this branch: the full set twice in a row into two scratch directories,
  `diff -r` of the fourteen JSONs: **empty** (`run-meta.json` differs in its `runAt` lines only).
  Screenshots: 46 of 46 byte-identical; that claim is for one machine and one Chrome, and the
  README's **Determinism** section, which owns the details, says what is and is not claimed.
  Against the committed `out/`, the same run changes 644 lines across the fourteen JSONs — 510
  are the id mask, 130 are sorts (28 `triggers` arrays, two `tokensOn`, two `steps[].tokensOn`,
  one `endEventsReached`; every one the same set reordered), and 4 are `consultation`'s
  `started.history` tail, where two concurrent tokens now arrive in release order rather than
  frame order; **none is a diagram change**: every `continued`, `continuedFirst` and
  breach/happy `history` is identical to the committed value. That measurement was over the
  fourteen goldens of the day and the pre-#430 fork shape; it is superseded by run 4, which
  repeats it over sixteen.

- **Run 4 — 2026-09-18 23:23:21 UTC (2026-09-19 07:23 SGT), all sixteen, one run.** Harness at
  `9cd110c5d` (which taught it the sub-process shape: end events found by type and placed as
  top-level or inside a box, no pause point on the multi-instance box, the breach scenario fires
  interrupting boundaries only — §1), committed as `a3c7bdce3` on the tree with
  legalese/l4-ide#430 merged in. **The committed `out/` is run 4 in full:** `run-meta.json`'s
  sixteen `perFixture` entries carry one `runAt`, `2026-09-18T23:23:21.042Z`, and one
  `harnessCommit`, `9cd110c5d` (`jq '.perFixture | map(.runAt) | unique'`: one value); the
  hand-written `perFixture` block and its `_note` from #425 are gone, the block is generated.
  **The "one run, all files from that run" rule is restored as of this run**, and it is now cheap
  to honour, because the acceptance test is a diff: the full set was run twice, back to back,
  and `diff -r -x '*.png' run4a run4b` names **one** file, `run-meta.json`, in 17 hunks, every one
  a `runAt` line (line 2 and the sixteen `perFixture` entries); no fixture JSON differs, and
  `cmp` on every PNG pair is **52 of 52 byte-identical**. Files: 16 fixture JSONs (121,983 bytes;
  `a3c7bdce3`'s message quotes 126,641, which is the seventeen JSONs with `run-meta.json`'s 4,658
  counted in), 52 PNGs (1,957,515 bytes; the largest `offering.png` at 57,995, none over 200 KB),
  `run-meta.json`. Per fixture: 3 PNGs (overview, happy, breach) for a single-arm fixture;
  `handover`, `regcf-advertising`, `regcf-resale` 4; `regcf-reporting` 5; `consultation` 2 (no
  breach boundary). `pageErrors` is `[]` in all sixteen; no JSON carries a machine path or a
  browser version; every `fixture` is repo-relative (`jl4/examples/bpmn/expected/<name>.bpmn`).
  Regenerated, not reconciled: the goldens moved twice between run 3 and run 4 (#425's lapse
  arm, #430's sub-process), so a run3-vs-run4 diff shows the goldens' churn and is not
  classified here; the twice-run diff above is the determinism claim. **Every `MEASURED` value
  below is read from run 4's JSON unless a row says which earlier run it quotes.** Two reading
  notes for older rows: arrays that report a set (`tokensOn`, `triggers`,
  `boundarySubscriptions`, `endEventsReached`) are sorted since `983d4a82a`, so a row that quotes
  run 3's `["Start_0","Task_0","Boundary_0"]` is the same set run 4 writes as
  `["Boundary_0","Start_0","Task_0"]`; and the log's scope ids are masked to `<scope-N>`.

**Every claim below is tagged.** `MEASURED` means the harness ran it and the value is in
`out/<fixture>.json` (field named where it matters) or visible in the linked screenshot. `READ`
means it comes from the simulator's source at `node_modules/bpmn-js-token-simulation/lib/…` (0.40.0, per `package.json`)
or its documentation, and was **not** exercised. A `READ` line is a prediction, not an observation.

---

## 1. Method, and the one charitable choice it makes

The simulator does not stop at a task. `ActivityBehavior.enter` exits the activity the moment a
token arrives unless a **pause point** has been set on it
(`lib/simulator/behaviors/ActivityBehavior.js:56,140`, READ). Left alone, a run from the start event
goes straight to an end event and there is no instant at which _"what is owed right now"_ could be
read off the picture at all. Pause points are the tool's own affordance — the pause icon that
appears on hover — so the harness sets one on **every** task before starting (`pausePoints` in
each JSON, MEASURED). That is the most charitable reading of the tool: a token sitting on a task is
then the closest thing the simulator has to "an obligation in force".

**Not on the fork's box, though.** Since #430 a fork is a multi-instance `bpmn:subProcess`
(`Scope_n`, "each Tenant"/"each Director") with the member's task inside it. The simulator would
honour a pause point on the box, but then the token parks on the box, the only triggers offered
are `Start_0`, the box itself and its escalation relay, and the task inside is not yet enterable
(`Task_0`'s pad offers only "Remove pause point") — a state that says nothing about the rule, and
one more click on every happy path. That probe is recorded in `9cd110c5d`'s commit message
(measured on `tenancy-fork.bpmn` at `646f58e9c`), not in any committed JSON, so it is cited and
not tagged MEASURED here. The committed run pauses the tasks only, so the token runs into the box
and stops on the member's task with the box still live: `started.tokensOn: ["Scope_0","Task_0"]`,
`started.instances: {"Scope_0": 1}`, `boundarySubscriptions: ["BoundaryEsc_0","Boundary_0"]`
(`tenancy-fork.json`, MEASURED). The tasks continued are exactly the elements paused, one list
(`pausePoints` equals `happy[0].continued` on all five fork fixtures — `["Task_0","Task_1"]` on
`tenancy-fork`, `["Task_4","Task_1"]` on `tenancy-fork-beside-party`, `["Task_0"]` on the two
one-task `modals-*-fork`s — MEASURED).

Three scenarios per fixture, each from a fresh start:

| scenario    | what the harness does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | screenshot                       |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| `started`   | fire the start event; read the state at 1.5 s; read it again at 4.5 s                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `out/<fixture>.png`              |
| `breach`    | continue activities until the simulator offers an **interrupting** boundary-event trigger — a timer or condition on the act, `cancelActivity="true"`; never the fork's non-interrupting escalation relay `BoundaryEsc_n`, which is offered from the moment the box is entered (`started.triggers`, all five fork JSONs, MEASURED) and, fired cold, reaches `EndBreach_n` with the task never entered (a probe recorded in `9cd110c5d`'s message, not in the JSON) — and fire it (exclusive-gateway arms tried in document order until one reaches a boundary) | `out/<fixture>.breach.png`       |
| `happy[-k]` | keep clicking "continue" on whatever waits, until nothing waits or 12 steps; once per arm `k` of the first exclusive gateway — arm 0 is the simulator's own default, first flow in document order                                                                                                                                                                                                                                                                                                                                                             | `out/<fixture>.happy[-armk].png` |

The step limit is 12 (`run-meta.json`'s `stepLimit`). Over run 4 the longest acyclic happy path
is 5 steps (`offering`), the fork fixtures need 1–2, and the only run to hit the limit is
`regcf-reporting`'s arm 2 (`happy[2].hitStepLimit: true`), which is a cycle cut by design (§3.6);
raising the limit would lengthen that one output and change nothing else (MEASURED, every
`happy[k].steps` length across `out/*.json`).

---

## 2. What the simulator handles at all (READ, then confirmed on the fixtures)

| element in the goldens                                          | simulator source                                                                                                                       | tag  | on these fixtures                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `task`, `businessRuleTask`                                      | `ActivityBehavior.js:17-25` registers Task, UserTask, BusinessRuleTask, … ; no wait unless paused                                      | READ | every task and every `Decide_0` took a token and paused (MEASURED, `started.tokensOn`)                                                                                                                                                                                                                                                                                                                          |
| `parallelGateway` split / join                                  | `ParallelGatewayBehavior.js` — joins only when every incoming flow has a scope waiting                                                 | READ | `consultation`'s `Join_0` waited for both branches and fired once (MEASURED, §3); `tenancy-fork-beside-party`'s `Split_0` ("all of") put one token on the box and one on the landlord's `Task_4` (`started.tokensOn: ["Scope_1","Task_1","Task_4"]`, MEASURED, run 4)                                                                                                                                           |
| `exclusiveGateway`                                              | `ExclusiveGatewayBehavior.js:24` — _"depends on UI to properly configure activeOutgoing"_                                              | READ | `conditionExpression` text was never consulted; the arm is whatever was last clicked, default arm 0 (MEASURED, §3, the three `regcf-*` and `handover`)                                                                                                                                                                                                                                                          |
| `boundaryEvent` + `timerEventDefinition`                        | `Simulator.js:390` — a timer is an event of `type: 'timer'` and nothing else; `:922` makes timer/condition boundaries user-triggerable | READ | no timer fired on its own in 4.5 s of wall clock; every timer was a "Trigger Event" play button from the moment its host had a token (MEASURED, `started.afterWaiting3s`, all sixteen, run 4)                                                                                                                                                                                                                   |
| `boundaryEvent` + `conditionalEventDefinition`                  | same as timer (`:922`) — a play button, the condition text is not evaluated                                                            | READ | `handover`'s `` `grace period` `` boundary and `regcf-advertising`'s _"unreachable: no WITHIN"_ boundary were both offered and both fired when clicked (MEASURED, §3)                                                                                                                                                                                                                                           |
| `endEvent` + `errorEventDefinition`                             | `EventBehaviors.js:154` — _"TODO(nikku): ensure error always interrupts, also if no error catch is present"_                           | READ | with no catcher, the error end consumed its own token and **siblings kept running** (MEASURED, `offering` §3). `tenancy-fork-beside-party` has one (`End_3`, the landlord's) and neither scenario reached it: the breach fired inside the box and left `Task_4` live (`breach.tokensOn: ["Task_4"]`, MEASURED, run 4)                                                                                           |
| `multiInstanceLoopCharacteristics` on a task                    | **no occurrence of `multiInstance` or `loopCharacteristics` anywhere under `lib/`** (grep, 0 hits, re-run 2026-09-19 on 0.40.0)        | READ | the three-bar marker renders; the simulator ran one scope, one token, one click on every barrier fixture (`tenancy-barrier`, the three `modals-*-barrier`: `started.tokensOn: ["Task_0"]`, `started.instances: {}`, MEASURED, run 4)                                                                                                                                                                            |
| `subProcess` + `multiInstanceLoopCharacteristics`               | `SubProcessBehavior.js:106,130` — `_findStarts` signals the box's own start event, once; the same grep says nothing about instances    | READ | the five fork fixtures' `Scope_n` ran as **one** instance: `started.instances: {"Scope_0": 1}` (`{"Scope_1": 1}` on `tenancy-fork-beside-party`), the box and the task inside both in `tokensOn`, `instances: {}` after the terminal step (MEASURED, run 4, §3.1, §3.7)                                                                                                                                         |
| `boundaryEvent` + `escalationEventDefinition`, non-interrupting | `EventBehaviors.js:99` — escalations propagate up the scope; `Simulator.js:225,404` — an event of `type: 'escalation'`, matched by ref | READ | the relay `BoundaryEsc_n` (`cancelActivity="false"`, the only non-interrupting boundary in the corpus: `elements[].cancelActivity`, all sixteen) subscribed the moment the box was entered and was offered as a trigger; on every fork breach it was exercised by the `EscScope_n` thrown inside (`breach.history`, MEASURED, run 4)                                                                            |
| `multiInstanceLoopCharacteristics` / `completionCondition`      | same grep, 0 hits: the simulator never reads the condition                                                                             | READ | bpmn-moddle **imports** it — the census records `completionCondition: "nrOfCompletedInstances >= 1"` on `modals-shant-barrier`'s `Task_0` and `null` on every other multi-instance element, `modals-shant-fork`'s `Scope_0` included (MEASURED, `elements[].multiInstance`, run 4; `grep completionCondition expected/*.bpmn` hits one file) — and the run is one scope, one token, one click regardless (§3.7) |
| `lane`, `participant`                                           | no lane behaviour; `Participant` is the root scope                                                                                     | READ | the token-count badge is per pool; lanes are paint (MEASURED, screenshots)                                                                                                                                                                                                                                                                                                                                      |
| unsupported                                                     | `ElementSupport.js:18-20` — only `bpmn:ComplexGateway`                                                                                 | READ | `unsupportedElements: []` on all sixteen; zero import warnings; zero page errors (MEASURED, run 4)                                                                                                                                                                                                                                                                                                              |

So the simulator accepts every golden without complaint. That is the same well-formedness trap
`jl4/examples/bpmn/README.md` warns about for `validate-bpmn.mjs`: acceptance is not evidence.

---

## 3. Case by case

Each row answers the §7.3 question — _what do I owe, what discharges it, what breaches it_ — from
the simulator's picture alone, and says where the picture stops. Screenshots are in
`etc/bpmn-token-sim/out/`.

### 3.1 `tenancy-barrier` and `tenancy-fork` — the pair that differed only by the join, and since #430 differs in shape

Source: `jl4/examples/bpmn/tenancy.l4`. `the tenancy` is `EVERY Tenant t IN tenants MUST Sign …
WITHIN 14 ONCE ALL HAVE HENCE (theLandlord MUST Deliver … WITHIN 5)`; `receipts` is `EVERY Tenant
t IN tenants MUST Pay … WITHIN 7 UPON EACH HENCE (theLandlord MUST Receipt … WITHIN 5) LEST BREACH
BY t`. Through run 3 their XML differed only in names, deadlines and the `<documentation>`
carrying `P-CAST`/`P-FORK` (13 changed lines), and the animation could not tell them apart. Since
legalese/l4-ide#430 the fork golden is a different shape: a multi-instance `bpmn:subProcess`
`Scope_0` ("each Tenant") holding `StartScope_0`, `Task_0`, `Task_1`, both timers, an `EndScope_0`
(Fulfilled) and an escalation-throwing `EscScope_0` (Breach); outside it, a non-interrupting
`BoundaryEsc_0` ("a member breached") relaying to `EndBreach_0`, and the box's own flow to
`EndGroup_0` ("every run has ended") — 12 census entries against the barrier's 7, 4 end events
against 2, and `diff` of the two goldens is now 227 changed lines. What the exporter claims for
that shape is #430's; what follows is what the simulator showed of it in run 4.

**Barrier vs fork, field by field (run 4, `tenancy-barrier.json` / `tenancy-fork.json`):**

| field                            | `tenancy-barrier`                                                    | `tenancy-fork`                                                                                                                                                                                                           | tag      |
| -------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| `started.tokensOn`               | `["Task_0"]`                                                         | `["Scope_0","Task_0"]` — the box is a token holder and stays live beside the task inside it                                                                                                                              | MEASURED |
| `started.instances`              | `{}` (no sub-process to count)                                       | `{"Scope_0": 1}` — **one** instance of the box, for a cast of three                                                                                                                                                      | MEASURED |
| `started.boundarySubscriptions`  | `["Boundary_0"]`                                                     | `["BoundaryEsc_0","Boundary_0"]` — the member's timer and the box's escalation relay, both armed from the first instant                                                                                                  | MEASURED |
| `started.triggers`               | `["Boundary_0","Start_0","Task_0"]`                                  | `["BoundaryEsc_0","Boundary_0","Start_0","Task_0"]` — the relay is offered as a play button too                                                                                                                          | MEASURED |
| `started.log`                    | _Process started · Start Event_                                      | _Process started · Start Event · each Tenant started · Start Event_ — two scopes, `<scope-1>` and `<scope-2>`                                                                                                            | MEASURED |
| `breach.fired`                   | `Boundary_0` (_after P14D_)                                          | `Boundary_0` (_after P7D_), inside the box                                                                                                                                                                               | MEASURED |
| `breach.history` after the timer | `Flow_Boundary_0__End_3, End_3`                                      | `Flow_Boundary_0__EscScope_0, EscScope_0, BoundaryEsc_0, Flow_BoundaryEsc_0__EndBreach_0, Flow_Scope_0__EndGroup_0, EndBreach_0, EndGroup_0`                                                                             | MEASURED |
| `breach.endEventsReached`        | `["End_3"]` — top level                                              | `["EndBreach_0","EndGroup_0","EscScope_0"]`: top level `["EndBreach_0","EndGroup_0"]`, inside `{"Scope_0": ["EscScope_0"]}` — the member's Breach, the relayed Breach **and** "every run has ended", all in the one step | MEASURED |
| `breach.log`                     | _MUST Sign … · after P14D · Breach · Process finished_               | _MUST Pay … · after P7D · Breach · a member breached · each Tenant finished · Breach · every run has ended · Process finished_                                                                                           | MEASURED |
| `happy[0].steps`                 | 2: `Task_0` then `Task_1`, `tokensOn` `["Task_0"]` then `["Task_1"]` | 2: `Task_0` then `Task_1`, `tokensOn` `["Scope_0","Task_0"]` then `["Scope_0","Task_1"]` — the landlord's `MUST Receipt` is inside the member's box                                                                      | MEASURED |
| `happy[0].endEventsReached`      | `["End_2"]` Fulfilled                                                | `["EndGroup_0","EndScope_0"]`: top level `["EndGroup_0"]`, inside `{"Scope_0": ["EndScope_0"]}`                                                                                                                          | MEASURED |
| `instances` after the last step  | `{}`                                                                 | `{}` on both scenarios — the one instance is gone, and `EndGroup_0` arrived in the same step as its `EndScope_0`/`EscScope_0`                                                                                            | MEASURED |
| pause points / continued         | `["Task_0","Task_1"]` / the same                                     | `["Task_0","Task_1"]` / the same — the box is never paused and never continued (§1)                                                                                                                                      | MEASURED |

So the pair is **no longer identical in the animation**: the fork shows a box with a count of one,
two live subscriptions, an inner Breach that becomes an outer Breach by relay, and two end events
where the barrier has one. What it still does not show is the `n`:

| question                              | what the simulator shows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | tag      |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| does the multi-instance task animate? | Barrier: yes, as **one** activity — one scope on `Task_0`, one token badge, one "Trigger Event" pad on the task and one on `Boundary_0`. Fork: yes, as **one instance** of the box — `instances: {"Scope_0": 1}`, one token on `Task_0` inside it. Neither says there are three tenants or how many have paid; the census records the box's `loopDataInputRef: true` and no `loopCardinality`, which is all the simulator was given to count with, and it counts nothing (§2, no MI behaviour).                            | MEASURED |
| does it wait? (barrier)               | For **one** click, not for `n`. One "continue" on `Task_0` moved the token to `Task_1` (`happy[0].continued: ["Task_0","Task_1"]`, ends at `End_2` Fulfilled). There is no state between "no tenant has signed" and "all have signed".                                                                                                                                                                                                                                                                                     | MEASURED |
| does it fire per member? (fork)       | Once — because there is one member. `Task_0 → Task_1 → EndScope_0`, then `EndGroup_0` in the same step (`happy[0].history`). "Every run has ended" is reached the instant the single run ends; the n-member story — the group's end waiting for the last member, other members still bound after one breaches — is not observable in this tool.                                                                                                                                                                            | MEASURED |
| what do I owe right now?              | "A token is on `MUST Sign …`" / "on `MUST Pay …` inside `each Tenant`". Who — Alice, Bob, Carol, or all of them — is not sayable on either; the lane label reads `EVERY Tenant t IN tenants` verbatim, and the box's count badge reads 1.                                                                                                                                                                                                                                                                                  | MEASURED |
| what discharges it?                   | Clicking the task. The act is the task label truncated to `MUST Sign ...` / `MUST Pay ...`.                                                                                                                                                                                                                                                                                                                                                                                                                                | MEASURED |
| what breaches it?                     | Clicking the timer. Barrier: `Boundary_0` → `End_3` Breach, _"Process finished"_. Fork: `Boundary_0` → `EscScope_0` inside the box → `BoundaryEsc_0` → `EndBreach_0` **and** `EndGroup_0`, _"a member breached … Breach … every run has ended"_. `WITHIN` never elapsed on its own on either; the timer was clickable from the first instant and still unfired 4.5 s later (`started.afterWaiting3s.tokensOn` unchanged). Which tenant's lateness breached (`LEST BREACH BY t`) is not sayable: `EndBreach_0` is one node. | MEASURED |
| the `LEST` arm's meaning              | The timer is labelled `after P14D`/`after P7D` and the arrow goes to a Breach end. That it is the **default** consequence of not acting — not an alternative route — is nowhere; a reader sees two exits of equal standing. On the fork the relay adds a third pad, `BoundaryEsc_0`, offered from the start; fired by hand it is a breach with no cause (§1), which is why the harness never fires it cold.                                                                                                                | MEASURED |
| the `HENCE` into the landlord's duty  | Barrier: sequence at the top level; the landlord's token appears only after the tenants' task completes — correct for `ONCE ALL HAVE`. Fork: `Task_1` (`MUST Receipt`) sits **inside** the box, so the picture now says the receipt follows each payment — the shape `UPON EACH` means — but the simulator runs the box once, so it is drawn per member and animated for one.                                                                                                                                              | MEASURED |

Screenshots: `tenancy-barrier.png` (token on the three-bar task, timer clickable), `tenancy-fork.png`
(token on `Task_0` inside the three-bar box, two pads); `*.breach.png` (after the timer);
`*.happy.png` (finished).

#### 3.1a `tenancy-fork-beside-party` — the fork `RAND` a `PARTY` obligation (new in run 4)

Source: `receipts and delivery` in `tenancy.l4` — the fork of `receipts` (without the receipt
`HENCE`, `HENCE FULFILLED`) beside `PARTY theLandlord MUST Deliver WITHIN 14 LEST BREACH`, under
`RAND`. The census has a parallel `Split_0` ("all of"), the box `Scope_1` with `Task_1`,
`Boundary_1` (_after P7D_), `EndScope_1`, `EscScope_1`, the relay `BoundaryEsc_1` and
`EndBreach_1`/`EndGroup_1`, and at the top level the landlord's `Task_4` with `Boundary_4` (_after
P14D_), `End_2` Fulfilled and `End_3`, an error end (15 entries, 6 end events).

| question                                | what the simulator shows                                                                                                                                                                                                                                                                                                                                                                                                                                                                | tag      |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| the `RAND`                              | Two live branches after `Split_0`: `started.tokensOn: ["Scope_1","Task_1","Task_4"]`, `instances: {"Scope_1": 1}`, three subscriptions (`BoundaryEsc_1`, `Boundary_1`, `Boundary_4`), six triggers. The parallel split is the one thing here the simulator animates exactly.                                                                                                                                                                                                            | MEASURED |
| the member's breach beside a live party | `breach.fired: "Boundary_1"` → `EscScope_1` → `BoundaryEsc_1` → `["EndBreach_1","EndGroup_1"]` at the top level, `{"Scope_1": ["EscScope_1"]}` inside — and `breach.tokensOn: ["Task_4"]`: the landlord's duty is **still live** after the member's breach, and the log ends _"every run has ended"_ with no _"Process finished"_. The plain `EndBreach_1` is not an error end, so it did not cancel `Task_4`; whether that is the rule's meaning is #430's claim, not the simulator's. | MEASURED |
| the happy path                          | 2 steps, `continued: ["Task_4","Task_1"]` (document order): `Task_4 → End_2` Fulfilled, then `Task_1 → EndScope_1 → EndGroup_1`; `endEventsReached: ["EndGroup_1","EndScope_1","End_2"]`, top level `["EndGroup_1","End_2"]`.                                                                                                                                                                                                                                                           | MEASURED |
| the landlord's error end `End_3`        | Present in the census, reached in neither scenario: the breach scenario fires the first interrupting boundary the simulator offered (`run.mjs`'s `liveBoundaryOf`, first match in the trigger list), which was the member's `Boundary_1`; `Boundary_4` is never fired. What an error end does to the fork's box is therefore **not measured** here (`offering`'s error end, §3.2, is the nearest exercised case, and it had no box to cancel).                                          | MEASURED |

### 3.2 `offering` — three parties, four-way `RAND`, two `SHANT`s, breach terminal

| question                                           | what the simulator shows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | tag      |
| -------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| the `RAND` (P-NOJOIN, fork without a join)         | After `Task_0`, `Split_1` put one token on each of `Task_2`, `Task_5`, `Task_6`, `Task_7` — four concurrent scopes in three lanes (`happy.steps[1].tokensOn`). That much is right and is the exhibit's whole point. There is no join, and the simulator does not miss it: each branch ends on its own.                                                                                                                                                                                                                                                                                                                                                                                                    | MEASURED |
| the `SHANT`s (`Task_5` advertise, `Task_7` resell) | **"Continue" on a prohibition is the breach.** On the happy run the harness clicked every waiting task; the log reads _"SHANT advertise the terms → Breach"_ and _"SHANT resell the securities → Breach"_ (`happy.endEventsReached: ["End_3","End_4","End_3","End_4"]`). The play button, the pad and the token look identical on a `MUST` and a `SHANT`; only the label's first word differs. F1 in motion.                                                                                                                                                                                                                                                                                              | MEASURED |
| the `SHANT`s' `HENCE` arm                          | Firing `Boundary_5`/`Boundary_7` ("after P30D, not performed") would go to Fulfilled — the timer is the **compliance** exit. Not exercised here (the breach scenario fires the first live boundary, `Boundary_0`); the same shape was exercised on `regcf-resale` below and behaves as read.                                                                                                                                                                                                                                                                                                                                                                                                              | READ     |
| the error end event                                | **Breach did not abandon the siblings.** After `Task_5 → End_4 (Breach)` the run continued: `Task_6 → Fulfilled`, `Task_7 → Breach`, then _"Process finished"_. Two Fulfilled and two Breach tokens, and the process "finished" as if nothing were wrong. jBPM aborts the instance here (`README.md` table: _ABORTED via error end event_); the simulator does not, per `EventBehaviors.js:154`'s own TODO. The picture's verdict is _finished_, not _breached_.                                                                                                                                                                                                                                          | MEASURED |
| what do I owe right now?                           | After the split: "tokens on four tasks". Which of the four is mine is the lane; that two of them are things I must **not** do is the label's first word.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | MEASURED |
| what discharges / breaches it?                     | For `MUST`: click = discharge, timer = breach. For `SHANT`: click = breach, timer = discharge. Same two buttons, opposite meanings, no notational difference.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | MEASURED |
| `BREACH BY Issuer BECAUSE "…"`                     | Not present. The Breach end is one node shared by five arrows; who breached and why is not recoverable from the picture.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | MEASURED |
| deadlines as boundary timers                       | Five timers drawn, none counting. Only the one on the task holding the token is offered — `Boundary_0` (`P21D`) with the token on `Task_0` (`started.triggers: ["Start_0","Task_0","Boundary_0"]`) — and it is offered from the first instant, not after 21 days. The harness records `triggers` only at start and after the 3 s wait, so which boundaries were offered after `Split_1` is **not** on record (`happy.steps[]` carries `tokensOn` alone); five at once is impossible in any case, `Task_0` having completed before `Split_1` fires. `P21D`, `P7D`, `P30D`, `P120D`, `P365D` are labels; a reader cannot tell from the animation that the resale restriction outlasts the filing by a year. | MEASURED |

Screenshots: `offering.png` (token on `Task_0`, five timers drawn, one play pad on `Boundary_0`), `offering.breach.png` (after `P21D`:
straight to Breach, no `RAND` ever started), `offering.happy.png` (finished; two branches breached).

### 3.3 `handover` — a named deadline, a `RAND`, a `ROR` of permissions, lapse timers

> **Re-measured 2026-09-17 on `handover` alone, and re-cut against run 4 (2026-09-18 23:23 UTC,
> all sixteen).** legalese/l4-ide#425's PARTY MAY lapse arm turned this file's four synthesised
> `Lapse_n` boundary events into ordinary `Boundary_n` ones — `L4.StateGraph` now draws a
> permission's lapse as a real `LEST` edge, so `L4.Bpmn.Lower` no longer synthesises a timer of
> its own. Run 4's census confirms the same ids: five `bpmn:BoundaryEvent`s, `Boundary_0`
> (_timeout_, conditional), `Boundary_2`/`Boundary_4` (_after P7D_, timers, each to `End_3`),
> `Boundary_6`/`Boundary_7` (_after P30D_, timers, each to `End_3`), every one
> `cancelActivity: true`, and no element whose id starts `Lapse` (`elements[]`, MEASURED). One
> end event, `End_3` Fulfilled; `exclusiveGateways: [{Split_5: [Flow_Split_5__Task_6,
Flow_Split_5__Task_7]}]`. Nothing else about the diagram moved: same shape, same flows, same
> terminals. Every row below is now read from run 4's `handover.json`; the 2026-09-17 per-fixture
> run it replaced measured the same values.

| question                               | what the simulator shows                                                                                                                                                                                                                                                                                                                                                                                                                                                        | tag              |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| the named deadline (`P-DEADLINE`)      | `Boundary_0` is a conditional boundary carrying `` `grace period` `` as text. The simulator offered it as a play button exactly like a timer (`started.triggers: ["Boundary_0","Start_0","Task_0"]`). That its duration is a _name_ the L4 source resolves to 14 is invisible; that it is not a timer at all is invisible.                                                                                                                                                      | MEASURED         |
| the `LEST` arm as a `ROR` of `MAY`s    | Firing `Boundary_0` moved the token through `Split_5` ("one of") to **`Task_6` only** (`breach.tokensOn: ["Task_6"]`, `breach.arm: "Flow_Split_5__Task_6"`, `MAY retain the deposit`), because the exclusive gateway's arm was the default first flow. In L4 a `ROR` of permissions is resolved by whichever act happens; here the reader must **choose the arm at the gateway before either permission is exercised**, and the other arm is greyed out.                        | MEASURED         |
| the `RAND` of `MAY`s with lapse timers | Both tokens sat on `Task_2` and `Task_4` (`happy[0].steps[1].tokensOn`) with `Boundary_2`/`Boundary_4` — the ordinary boundary events that replaced `Lapse_2`/`Lapse_4` on 2026-09-17 — hung on them. Continuing both reached Fulfilled twice (`happy[0].endEventsReached: ["End_3","End_3"]`, and the same on arm 1). A permission not exercised is still not representable except by clicking its timer; there is no "decline", and renaming the element did not give it one. | MEASURED (run 4) |
| what do I owe right now?               | After `Boundary_0`: a token on `MAY retain the deposit`. Nothing says the tenant is in breach — there is **no breach end in this file** (`Split_5` leads only to Fulfilled), so the animation of the tenant's failure ends in _"Fulfilled"_.                                                                                                                                                                                                                                    | MEASURED         |
| what discharges / breaches it?         | For the `MAY`s: click or timer, both to Fulfilled — the picture correctly shows there is nothing to breach, and incorrectly makes the permission look like a task to be done.                                                                                                                                                                                                                                                                                                   | MEASURED         |
| `P-NOJOIN` on `Split_1`                | No join drawn; both permission tokens end independently. Correct by construction and invisible as a loss.                                                                                                                                                                                                                                                                                                                                                                       | MEASURED         |

Screenshots: `handover.png`, `handover.breach.png` (token on the chosen `ROR` arm, other arm
greyed), `handover.happy-arm0.png` / `-arm1.png` (identical: the gateway is only on the `LEST` path).

### 3.4 `consultation` — the only drawn join

| question                        | what the simulator shows                                                                                                                                                                                                                                                                          | tag      |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| the converging parallel gateway | It works as a barrier: tokens on `Task_1` and `Task_4`; continuing `Task_1 → Task_2` then `Task_4` fired `Join_0` once and reached `End_3` (`happy.log` ends _"MAY file a response, Parallel Gateway, Fulfilled, Process finished"_). The one join P1 can draw, the simulator animates correctly. | MEASURED |
| three `MAY`s, no deadlines      | Every one is a task with a play button. The only way the process finishes is to **perform all three permissions**. That the whole thing is optional — a `RAND` of `MAY`s — has no expression: a reader who does nothing sees two tokens parked forever and no exit.                               | MEASURED |
| what do I owe right now?        | "Tokens on two `MAY`s". Nothing is owed; the picture cannot say so (F1, `[F1] blocking` ×3 in the fidelity report).                                                                                                                                                                               | MEASURED |
| what breaches it?               | Nothing can, and the picture agrees by having no Breach node. This is the one fixture where the simulator's silence is the right answer.                                                                                                                                                          | MEASURED |

### 3.5 `regcf-advertising` and `regcf-resale` — DMN-wired gateway, a `SHANT`, an undrawable `LEST`

Both have the same shape: `Start → Decide_0 (businessRuleTask) → Split_0 (XOR) → {Fulfilled | SHANT task}`.

| question                                                                 | what the simulator shows                                                                                                                                                                                                                                                                                                                                                                                                                                                       | tag      |
| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| the `businessRuleTask` (`P-DMNWIRED`)                                    | A token parks on `Decide_0` and waits for a click. The DMN decision is not evaluated — the simulator has nothing to evaluate it with — and the `conditionExpression`s on the arms (`notice_complies_with_Rule_204_b` / `not(…)`) are never read. The arm is a **setting on the gateway**, chosen by the reader.                                                                                                                                                                | MEASURED |
| the `IF` guard's meaning                                                 | Lost. Arm 0 (Fulfilled) and arm 1 (the `SHANT`) are equally available; nothing says arm 0 is "the notice complies" and arm 1 is "it does not". The guard text lives in the flow's `<documentation>`, which the simulator does not display.                                                                                                                                                                                                                                     | MEASURED |
| the `SHANT` (`Task_2`)                                                   | Arm 1, continue: _"SHANT advertise the terms of the offering → Breach"_ (`happy[1].endEventsReached: ["End_3"]`). Same as `offering`: the play button is the violation.                                                                                                                                                                                                                                                                                                        | MEASURED |
| the `LEST` with no `WITHIN` (`P-DEADLINE` blocking, `regcf-advertising`) | **The simulator let the harness fire an event the fidelity report says nothing can fire.** `Boundary_2` is named _"unreachable: no WITHIN"_; it was offered as a trigger and, clicked, went to `End_1` Fulfilled (`breach.fired: "Boundary_2"`, `breach.endEventsReached: ["End_1"]`). The animation reads: "the prohibition can be discharged by waiting" — and the rule sets no period to wait for. A standing prohibition for the life of the offering animates as a timer. | MEASURED |
| `regcf-resale`'s named deadline boundary                                 | `Boundary_2` (_"deadline passes, not performed"_, condition `` `days until the first anniversary …` OF transfer ``) fired → Fulfilled. Correct polarity for a `SHANT` (waiting out the period is compliance), and the reader cannot tell that from the identical-looking `MUST` timers elsewhere.                                                                                                                                                                              | MEASURED |
| what do I owe right now?                                                 | Before the gateway: "a token on a decision". Whether I am bound at all depends on the DMN answer the simulator cannot compute; the reader picks.                                                                                                                                                                                                                                                                                                                               | MEASURED |

### 3.6 `regcf-reporting` — the renewing obligation, `P-CYCLE`, a three-arm gateway

| question                          | what the simulator shows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | tag      |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| the cycle (`P-CYCLE`)             | Animates. Arm 2 (`MUST file a Form C-AR annual report and continue`) ran `Decide_0 → Task_4 → Decide_0 → Task_4 …` for twelve clicks and hit the harness's step limit with the token back on `Decide_0` and no end event reached (`happy[2].hitStepLimit: true`, `happy[2].endEventsReached: []`). The loop never exits by itself because the gateway arm is a fixed setting; a reader has to re-click the gateway to "decide" the year the reporting ends.                                                                                                                                                                                                                                                                                                                                                                                                                      | MEASURED |
| the layout `P-CYCLE` warns about  | Visible: the start event sits at the far left of a very wide pool and every other node at the far right, so the only thing the horizontal axis carried — time — reads as one long flow followed by a knot (`regcf-reporting.png`). This is P1's layout, not the simulator's, and the simulator draws over it faithfully.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | MEASURED |
| the three-arm exclusive gateway   | `Split_0` is `Diverging` (`expected/regcf-reporting.bpmn:32`, `elements[Split_0].gatewayDirection`); the loop re-enters at `Decide_0`, not at the gateway. bpmn-js renders it and the simulator routes through it. (jBPM still rejects the file, re-measured 2026-09-15 with `etc/check-bpmn-kie.sh`: _"This type of node [Decide_0 …] cannot have more than one incoming connection!"_ — the two arrivals at `Decide_0`, not a mixed gateway; `README.md`'s _"Unknown gateway direction: Mixed"_ predates the 2026-08-02 reroute and is corrected in the same commit as this line.) Its three arms are one setting cycled by clicking; arm 1, "fulfilled", is `Split_0 → End_2` with no task in between, so on that arm the process finishes after one click on `Decide_0` and _nothing was ever owed_ — which is right, and indistinguishable from a process that was skipped. | MEASURED |
| named deadlines (`P-DEADLINE` ×2) | `Boundary_1`/`Boundary_4` (_"timeout"_, conditions `` `business days to file Form C-TR` `` etc.) are play buttons. Firing `Boundary_1` → Breach (`breach.endEventsReached: ["End_3"]`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | MEASURED |
| what do I owe right now?          | On arm 2, after year `k`: "a token on `Decide_0`" — the same picture as year 1. Which fiscal year, how many reports have been filed, whether the obligation has ended: not sayable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | MEASURED |

### 3.7 The six `modals-*` goldens — RE-MEASURED 2026-09-15 17:19:57 UTC (2026-09-16 SGT); `modals-may-fork` RE-MEASURED 20:25:02 UTC after `d544ed22`; the three forks RE-CUT against run 4 (2026-09-18 23:23 UTC) after #430 made them sub-processes

Source: `jl4/examples/bpmn/modals.l4`, one rule per modal × join cell that `tenancy.l4` does not
cover, cut after the 2026-09-15 concurrency review found the multi-instance marker inverted the
`SHANT` barrier and the `MAY` barrier (LTS-VISUALISER.md §4.9 review block;
`jl4/examples/bpmn/README.md:98-103`). What the fix put in the files, and what this run asks of
it: a `completionCondition nrOfCompletedInstances >= 1` on both `SHANT` tasks (`P-PROHIBITION-FIRST`),
and a `LEST`-to-Fulfilled lapse timer on the barrier-joined `MAY` in place of a lapse routed into
the chair's duty — and, since `d544ed22`, on the fork-joined `MAY` too. The three barrier files
are each one multi-instance task with one boundary timer, plus (on the `MAY`) the chair's `MUST
Publish` with its own timer. The three fork files were the same shape through run 3; since #430
each is a multi-instance sub-process `Scope_0` ("each Director") holding the member's task, its
timer and (on the `MAY`) the chair's task, with `EndScope_0`/`EscScope_0` inside and
`EndGroup_0`/`EndBreach_0`/`BoundaryEsc_0` outside — the shape §3.1 describes on `tenancy-fork`.
All six: zero import warnings, zero unsupported elements, zero page errors, one pause point per
task and none on the box (MEASURED, run 4, `importWarnings`, `unsupportedElements`, `pageErrors`,
`pausePoints`).

**The three questions this re-run was asked, answered first:**

| question                                                                               | answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | tag             |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- |
| does the `SHANT`'s `completionCondition` change what the simulator does on "continue"? | **No.** bpmn-moddle imported it — `elements[Task_0].multiInstance.completionCondition` is `"nrOfCompletedInstances >= 1"` on both `modals-shant-*` and `null` on the other four and on `tenancy-*` — and the run is the same one scope, one token, one click as every other multi-instance task: `happy.steps: [{tokensOn: ["Task_0"], continued: "Task_0"}]`, then `End_2` Breach, _"SHANT Sublet … → Breach → Process finished"_. The condition can make no difference here because the simulator never runs `n` instances for it to complete early: `grep -r completionCondition node_modules/bpmn-js-token-simulation/lib/` is 0 hits, as is `multiInstance`/`loopCharacteristics` (§2). The engine case the condition guards against — waiting for _every_ member to offend — cannot arise in a simulator that has one member. **Run 4 narrows the first clause:** the condition is now on `modals-shant-barrier`'s `Task_0` only; `modals-shant-fork`'s multi-instance element is the box `Scope_0`, and its `completionCondition` is `null` (`elements[].multiInstance`, both JSONs; `grep completionCondition expected/*.bpmn` hits one file). The fork's `SHANT` runs as one instance of the box: `Task_0 → EscScope_0 → BoundaryEsc_0 → EndBreach_0 + EndGroup_0` (`happy[0].history`), _"SHANT Sublet … · Breach · a member breached · each Director finished · Breach · every run has ended"_ — the one member's offence is the group's breach, which is what one member makes trivially true.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | MEASURED + READ |
| does the `MAY`'s lapse arm animate?                                                    | **Yes, on both, to the same place — Fulfilled — and the fork's animation now agrees with the runtime.** `modals-may-barrier`: `breach.fired: "Boundary_0"` (_"after P30D"_) → `End_2` **Fulfilled**, _"Process finished"_, no token left (`breach.tokensOn: []`); the resolution that did not pass creates no duty to publish, which is what the fix intended. `modals-may-fork`, on the golden `d544ed22` replaced: `breach.fired: "Boundary_0"` (_"after P30D"_) → `End_2` **Fulfilled**, _"Process finished"_, `breach.tokensOn: []`, `breach.triggers: ["Start_0"]`, `breach.endEventsReached: ["End_2"]` — and the census now has `Boundary_0` with `outgoing: ["Flow_Boundary_0__End_2"]` where the second run had `Lapse_0` with `outgoing: ["Flow_Lapse_0__Task_1"]`, so `Task_1` (`MUST Publish`) has one incoming flow, from `Task_0`. With the ids masked, the two `modals-may-*` JSONs differ in the `fixture` path and in nothing else. That is what the runtime does: measured 2026-09-16 with two `#TRACE`s on `ok/every/run-modals.l4` (§7, `each approval is published` — the same rule as the fixture's) and their goldens (`ok/every/tests/run-modals.golden`): nobody approves and the chair publishes at day 40 ⇒ **FULFILLED** — no duty to publish ever arose; one director approves at day 3 and nobody publishes ⇒ **BREACHED**, the chair's, deadline day 8 — the `HENCE` is armed by the act and by nothing else (`Machine.hs:1877-1881`, the `DMay` arm of the expiry case, re-anchored 2026-09-16: a `MAY`'s expiry goes to `LEST`, default `FULFILLED`, whatever the join). **The second run (17:19 UTC) measured the opposite animation on the old golden** — `Lapse_0` → a token on `Task_1` with `Boundary_1` offered, the chair under a five-day duty because nobody approved — and that is what led to the fix: this row's finding was reproduced on `lts/p2a-remeasure` (`11199a16`) and folded into legalese/l4-ide#395 as `d544ed22`, which widens the `DMay` arm of `StateGraph.hs`'s `extractDeonton` from `Barrier _` to `Just _`, so the state graph draws a quantified `MAY`'s lapse as a `LEST` arm to Fulfilled under either join and `Lower.hs`'s `lapses` synthesis (_"wherever HENCE lands"_) no longer fires for it. The `PARTY … MAY` shape that `Lower.hs`'s `KNOWN WRONG` note measured was untouched by that commit; legalese/l4-ide#425 fixed it and `option` (§3.8) is its witness. **Run 4, after #430:** the two `modals-may-*` JSONs no longer differ in the path alone — the fork is a box — but the lapse still lands on Fulfilled: `modals-may-fork` `breach.fired: "Boundary_0"` → `EndScope_0` **inside** `Scope_0`, then `EndGroup_0` (`breach.endEventsReached: ["EndGroup_0","EndScope_0"]`, top level `["EndGroup_0"]`, inside `{"Scope_0": ["EndScope_0"]}`), `EscScope_0` and `EndBreach_0` untouched, `breach.tokensOn: []`; log _"MAY Approve … · after P30D · Fulfilled · each Director finished · every run has ended · Process finished"_. A permission lapsing is not a breach, on the fork as on the barrier, and the animation still agrees with the runtime. | MEASURED        |
| does `modals-must-barrier-both-deadlines` show two timers?                             | **No — one.** The census has exactly one `bpmn:BoundaryEvent`, `Boundary_0` _"after P30D"_ (`elements`, five entries: `Start_0`, `Task_0`, `End_1`, `End_2`, `Boundary_0`), and the screenshot draws one clock on the task. The join line's `WITHIN 10` is not in the file: `P-JOIN-DEADLINE` (lossy) in `modals-must-barrier-both-deadlines.fidelity.txt` says so — _"this exporter draws one timer per obligation"_ — and the `<documentation>` on `Task_0` is the only place the `10` survives, which the simulator does not display. Its fork twin `modals-must-fork-join-deadline` also has one timer, `Boundary_0` _"after P10D"_: there the join line's deadline **is** the member's timer (`P-FORK` + `P-FORK-CANCEL`, no `P-JOIN-DEADLINE`). Through run 3 the two JSONs differed in the fixture path and the two occurrences of that label and in nothing else (`diff` with ids masked: 3 changed lines); in run 4 the fork is a box (10 census entries against the barrier's 5, `Boundary_0` inside `Scope_0`, the relay `BoundaryEsc_0` outside), and its one timer still reads `after P10D` — `breach.fired: "Boundary_0"` → `EscScope_0` → `EndBreach_0` + `EndGroup_0`, the `tenancy-fork` breach shape of §3.1. So the simulator shows one deadline on each and cannot show that `quorum by ten` has two, or that its second one — the one the runtime breaches on (`ok/every/tests/run-modals.golden`: _"the last of them acted after the ONCE line's WITHIN deadline"_) — is the tighter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | MEASURED        |

**Per fixture.** `Task_0` is the member's task in all six and `Boundary_0` its timer; on the three barriers `Task_0` is itself the multi-instance element, on the three forks (run 4) it sits inside the multi-instance box `Scope_0`.

| fixture                              | cell, from `modals.l4`                                                       | `started` (1.5 s; unchanged at 4.5 s)                                                                                                                                  | `breach` — first live boundary, fired                                                                                                                                                                                                                                                                                                                                     | `happy` — "continue" on every waiting task                                                                                                                                                                                                                                                           | what the picture cannot say                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | tag              |
| ------------------------------------ | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `modals-may-barrier`                 | `MAY Approve WITHIN 30 ONCE ALL HAVE HENCE (theChair MUST Publish WITHIN 5)` | token on `Task_0`; triggers `Start_0`, `Task_0`, `Boundary_0`                                                                                                          | `Boundary_0` → `End_2` Fulfilled; `tokensOn: []`; log _"MAY Approve … · after P30D · Fulfilled · Process finished"_                                                                                                                                                                                                                                                       | `Task_0` → `Task_1` → `End_2` Fulfilled (2 steps); log _"MAY Approve … · MUST Publish … · Fulfilled"_                                                                                                                                                                                                | The happy path is the only way to reach the chair's duty, and it takes **one** click on a task labelled `MAY` — nothing says two directors must both approve (`ONCE ALL HAVE`), nothing says either may decline. The lapse to Fulfilled reads as a second way to finish, of equal standing with approving; that it is the _default_ if nobody acts is not marked. A `MAY` that lapses and a `MUST` that is breached are the same clock icon in different files.                                                                                                                                                                                 | MEASURED         |
| `modals-may-fork`                    | same, `UPON EACH`                                                            | token on `Task_0` inside the box `Scope_0`; `tokensOn: ["Scope_0","Task_0"]`, `instances: {"Scope_0": 1}`; triggers `BoundaryEsc_0`, `Boundary_0`, `Start_0`, `Task_0` | `Boundary_0` (_after P30D_) → `EndScope_0` inside, `EndGroup_0` outside — Fulfilled, not Breach; `tokensOn: []`, `instances: {}`; log _"MAY Approve … · after P30D · Fulfilled · each Director finished · every run has ended · Process finished"_ (run 4; run 3's golden, a task, went `Boundary_0 → End_2`; run 2's sent `Lapse_0` to `Task_1` — second question above) | `Task_0` → `Task_1` → `EndScope_0` + `EndGroup_0` (2 steps); both tasks inside the box; `endEventsReached: ["EndGroup_0","EndScope_0"]`                                                                                                                                                              | **No longer identical to the barrier**: the fork shows a box with a count of one, a second subscription (`BoundaryEsc_0`), and two end events per exit where the barrier has one — but the same two clicks and the same lapse, so the join is visible as a shape and invisible as a cardinality. `UPON EACH`'s once-per-member continuation is drawn (the chair's `MUST Publish` is inside the box) and animated once. The timer is interrupting (`cancelActivity: true`); with one instance nothing is there for it to cancel besides that instance. `EscScope_0`/`EndBreach_0` exist in the census and are reached on no path a `MAY` offers. | MEASURED (run 4) |
| `modals-shant-barrier`               | `SHANT Sublet WITHIN 30 ONCE ALL HAVE HENCE FULFILLED LEST BREACH`           | token on `Task_0`; triggers `Start_0`, `Task_0`, `Boundary_0` (_"after P30D, not performed"_)                                                                          | `Boundary_0` → `End_1` **Fulfilled**; log _"SHANT Sublet … · after P30D, not performed · Fulfilled · Process finished"_                                                                                                                                                                                                                                                   | `Task_0` → `End_2` **Breach** (1 step); log _"SHANT Sublet … · Breach · Process finished"_                                                                                                                                                                                                           | Same as `offering`'s `SHANT`s (§3.2): the play button is the violation, the timer is compliance, and the picture is a task with a play button. `completionCondition` imported, never consulted (first question). Who sublet is not sayable — one task, one lane labelled `EVERY Director d IN board`.                                                                                                                                                                                                                                                                                                                                           | MEASURED         |
| `modals-shant-fork`                  | same, `UPON EACH … LEST BREACH BY d`                                         | token on `Task_0` inside `Scope_0`; `instances: {"Scope_0": 1}`; triggers `BoundaryEsc_0`, `Boundary_0` (_after P30D, not performed_), `Start_0`, `Task_0`             | `Boundary_0` → `EndScope_0` inside, `EndGroup_0` outside — **Fulfilled**; log _"SHANT Sublet … · after P30D, not performed · Fulfilled · each Director finished · every run has ended · Process finished"_                                                                                                                                                                | `Task_0` → `EscScope_0` inside → `BoundaryEsc_0` → `EndBreach_0` + `EndGroup_0` (1 step) — **Breach**; `endEventsReached: ["EndBreach_0","EndGroup_0","EscScope_0"]`; log _"SHANT Sublet … · Breach · a member breached · each Director finished · Breach · every run has ended · Process finished"_ | **Inverted, as on the barrier**: the "happy" continue is the breach and the deadline passing is compliance — here with the extra step that the member's Breach is thrown as an escalation and caught outside as the group's. `BREACH BY d` is a label in the source and one `EndBreach_0` node in the file; with one instance, whose breach is not a question the picture can be asked. `completionCondition` is `null` on the box (first question).                                                                                                                                                                                            | MEASURED (run 4) |
| `modals-must-barrier-both-deadlines` | `MUST Approve WITHIN 30 ONCE ALL HAVE WITHIN 10 HENCE FULFILLED`             | token on `Task_0`; triggers `Start_0`, `Task_0`, `Boundary_0` (_"after P30D"_)                                                                                         | `Boundary_0` → `End_2` Breach; log _"MUST Approve … · after P30D · Breach · Process finished"_                                                                                                                                                                                                                                                                            | `Task_0` → `End_1` Fulfilled (1 step)                                                                                                                                                                                                                                                                | One timer, the member's 30; the join's 10 is absent (third question). One click fulfils a barrier that in L4 needs two approvals inside ten days; the run the golden breaches (`b` approves at 20) is a `MUST` that _was_ performed, inside its own 30 — a breach the picture has no way to show, since the only timer is the one that was not exceeded.                                                                                                                                                                                                                                                                                        | MEASURED         |
| `modals-must-fork-join-deadline`     | `MUST Approve UPON EACH WITHIN 10 HENCE FULFILLED LEST BREACH BY d`          | token on `Task_0` inside `Scope_0`; `instances: {"Scope_0": 1}`; triggers `BoundaryEsc_0`, `Boundary_0` (_after P10D_), `Start_0`, `Task_0`                            | `Boundary_0` → `EscScope_0` inside → `BoundaryEsc_0` → `EndBreach_0` + `EndGroup_0` — Breach; `endEventsReached: ["EndBreach_0","EndGroup_0","EscScope_0"]`; log _"MUST Approve … · after P10D · Breach · a member breached · each Director finished · Breach · every run has ended · Process finished"_                                                                  | `Task_0` → `EndScope_0` + `EndGroup_0` (1 step) Fulfilled                                                                                                                                                                                                                                            | The `tenancy-fork` shape of §3.1 with one task. Against its barrier twin `modals-must-barrier-both-deadlines` (one task, one timer, `End_1`/`End_2`) the fork now differs in shape, not just in the label `P10D` vs `P30D` — but that label is still the only place the picture carries a deadline, and it is the _join line's_ deadline on the fork and the _member's_ on the barrier, which nothing in the picture distinguishes. `BY d` is not present.                                                                                                                                                                                      | MEASURED (run 4) |

Screenshots: `modals-<cell>.png` (barriers: token on the three-bar task, one clock; forks: token
on the task inside the three-bar box, the relay's pad on the box), `modals-<cell>.breach.png`
(after the timer — on both `modals-may-*` the process has finished at Fulfilled), `modals-<cell>.happy.png`.

**What the six add to §4.** Two rows, appended there: the lapse of a permission, and the completion
condition. And one thing they confirm at larger `n`: with these six, and run 4's `option` and
`tenancy-fork-beside-party`, **twelve of sixteen** fixtures have a boundary on their first task —
every one but `consultation` and the three `regcf-*`, whose first activity is a gateway-fed
`Decide_0` — measured by filtering `started.triggers` for `Boundary*` across every `out/*.json`
(run 3 counted ten of fourteen the same way; the two newcomers both carry one), and on
every one of the twelve that boundary was offered at 1.5 s and `started.afterWaiting3s.tokensOn`
/ `.triggers` were unchanged at 4.5 s — the "no clock" finding of §4 held on every new file.

### 3.8 `option` — one `PARTY … MAY` whose `HENCE` is another party's duty (added by #425, first in a full run here)

Source: `jl4/examples/bpmn/option.l4`: `PARTY theBuyer MAY Exercise WITHIN 30 HENCE (PARTY
theSeller MUST Transfer WITHIN 14)`. The census has seven entries: `Task_0` (`MAY Exercise …`)
with `Boundary_0` (_after P30D_) whose outgoing flow is `Flow_Boundary_0__End_2`, `Task_1`
(`MUST Transfer …`) with `Boundary_1` (_after P14D_) to `End_3`, `End_2` Fulfilled, `End_3` Breach
(error). No multi-instance, no gateway.

| question                    | what the simulator shows                                                                                                                                                                                                                                                                                                                                      | tag      |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| the lapse of the permission | `breach.fired: "Boundary_0"` → `End_2` **Fulfilled**, `tokensOn: []` — letting the option expire obliges nobody, which is the destination #425 gave the lapse (the file's own header records that until 2026-09-17 the lapse was routed into the seller's duty). The harness's "breach" scenario therefore ends Fulfilled here, as on the two `modals-may-*`. | MEASURED |
| the `HENCE`                 | `Task_0 → Task_1 → End_2` (2 steps): exercising creates the seller's duty and its `P14D` timer. Same clock icon on the lapse and on the seller's breach timer; which one ends Fulfilled and which Breach is the arrowhead's destination and nothing else.                                                                                                     | MEASURED |
| what do I owe right now?    | "A token on `MAY Exercise …`" — the same token as on any `MUST`; that nothing is owed until it is exercised is the label's first word.                                                                                                                                                                                                                        | MEASURED |

---

## 4. Across all sixteen: what the simulator cannot say

Stated against §7.3's three clauses, with the `STATEFUL-CONTRACT-DEPLOYMENT.md` §6.4 endpoint that
would carry the answer as data.

| the clause                                     | endpoint      | the simulator's nearest thing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | tag      |
| ---------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| _what do I owe right now_                      | 14/15/16 + 17 | A token on an activity, provided someone set a pause point there. It is the same token on a `MUST`, a `MAY`, a `SHANT` and a decision (`Decide_0`). No bearer beyond the lane. No count under an `EVERY` — on the five fork fixtures the box's `instances` reads `1` for a cast of three (run 4). No "not applicable" or "never triggered" state — a norm that was never reached has no token and looks the same as one already discharged. No next deadline: timers are labels.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | MEASURED |
| _what would discharge it_                      | 19            | The play button on the task — for a `MUST`. For a `SHANT`, the play button on its **timer**. For a `MAY`, either. The simulator offers the same two buttons in all three cases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | MEASURED |
| _what would put me in breach_                  | 20            | The play button on the timer — for a `MUST`. For a `SHANT`, the play button on the **task**. On `offering`, a breach on one branch did not stop the others, and the process "finished". On `handover` there is no breach node at all. On the fork fixtures (run 4) the member's timer throws `EscScope_n` inside the box, the relay `BoundaryEsc_n` turns it into `EndBreach_n` outside, and `EndGroup_n` ("every run has ended") lands in the same step — one member's breach is the group's end, because there is one member; on `tenancy-fork-beside-party` the landlord's `Task_4` stays live through it. Who breached and why (`BREACH BY … BECAUSE …`) is never present: `EndBreach_n` is one node.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | MEASURED |
| the norm's lifecycle (§2.3, Symboleo's states) | —             | Two: token present / token absent. No _Created_ vs _Fulfilled_ vs _Violated_ vs _Expired_; a scope is `running`, `completed` or `failed` and the picture shows only the first.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | MEASURED |
| the barrier vs the fork (§4.9)                 | —             | **Distinguishable in shape since #430, not in cardinality** (run 4, all four pairs — `tenancy-*`, `modals-shant-*`, `modals-must-*`, `modals-may-*`). The fork draws a multi-instance box: `tokensOn` holds the box and the task inside it, `instances` is `{"Scope_0": 1}`, a second subscription (`BoundaryEsc_0`) is armed from the start, and every exit is two end events — `EndScope_0` or `EscScope_0` inside, `EndGroup_0` (and `EndBreach_0` on a breach) outside — where the barrier has one task, one token, one subscription and one end. Which end the animation reaches per pair, breach / happy: `tenancy` `EndBreach_0`+`EndGroup_0` / `EndGroup_0`; `modals-must` the same; `modals-may` `EndGroup_0` / `EndGroup_0` (the lapse is `EndScope_0`, Fulfilled — a permission lapsing is not a breach); `modals-shant` `EndGroup_0` / `EndBreach_0`+`EndGroup_0` (inverted: doing the act is the breach). But the box runs **once**: one click, one instance, `instances: {}` after the terminal step, and `EndGroup_0` in the same step as the one instance's end. `ONCE ALL HAVE` still waits for nothing and `UPON EACH` still fires once; the join is visible as a shape and invisible as an `n`. Through run 3 the pairs were not distinguishable at all (one task, one token, one click on both; the `modals-may-*` exception of the second run closed with `d544ed22`). | MEASURED |
| the `LEST` arm's default character             | —             | Not marked. Every boundary is a play button of equal standing with the task's own.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | MEASURED |
| guards (`PROVIDED`, `IF`, DMN)                 | —             | Not evaluated; the reader picks the arm. `P-DMNWIRED`'s `businessRuleTask` is a task that waits for a click.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | MEASURED |
| time                                           | —             | None. No clock, no countdown, no ordering of deadlines; every boundary that was offered was offered the instant its host task held a token, whatever its duration (`P7D`, `P14D`, `P21D` and `handover`'s named _timeout_ — the `started.triggers` of the four original fixtures whose first task carries a boundary; `P30D` ×5 and `P10D` on the six `modals-*`, §3.7; `P30D` on `option`; `P7D` inside the box and `P14D` beside it on `tenancy-fork-beside-party`), and none fired unaided in 4.5 s (`started.afterWaiting3s.tokensOn` unchanged on all twelve, run 4).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | MEASURED |
| unreachable events                             | —             | Fired on request. `regcf-advertising`'s _"unreachable: no WITHIN"_ boundary fires to Fulfilled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | MEASURED |
| the lapse of a permission                      | —             | A clock, like a breach. On both `modals-may-*` it ends the process Fulfilled (on the fork via `EndScope_0` inside the box and `EndGroup_0` outside, run 4; and on `option`, §3.8), which is what the runtime does (`ok/every/run-modals.l4` §7: nobody approves ⇒ FULFILLED); on the second run the fork's had put a token on the chair's `MUST Publish`, a duty the runtime never creates from a lapse, until `d544ed22` (§3.7, second question). Nothing marks the lapse as "nobody acted", and a `MAY` that lapses and a `MUST` that is breached are the same clock icon.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | MEASURED |
| the completion condition on a `SHANT`          | —             | Imported, never consulted, and unobservable: the simulator has one instance, so "first to complete" and "last to complete" are the same click (§3.7, first question). Since #430 only the barrier carries it (`modals-shant-barrier`'s `Task_0`); the fork's box has `completionCondition: null` (run 4).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | MEASURED |

And two things it says **well**, recorded so the baseline is not a straw man:

- **Concurrency.** Four tokens in three lanes on `offering` is a genuinely better answer to "what is
  live" than the static picture, and it is exactly what P1's `RAND` was drawn to show.
- **The one drawn join.** `consultation`'s `Join_0` animates as a barrier, correctly.
- **The escalation relay (run 4).** On every fork breach the member's `EscScope_n` was caught by
  the non-interrupting `BoundaryEsc_n` and re-thrown as `EndBreach_n` (`breach.history`, all five),
  and on `tenancy-fork-beside-party` it did so **without** cancelling the landlord's live `Task_4`
  (`breach.tokensOn: ["Task_4"]`) — the one-member version of "a member's breach must not end the
  members who did not breach" animates as drawn. What the group's end means with one member is a
  different matter (§4, the barrier vs the fork).

---

## 5. What this cannot falsify (§7.2's second table, against what was observed)

§7.2 says P2a tests _"position, as an off-the-shelf animation over BPMN"_ and **cannot** test
reachability or dominance. Both held, and the reasons were visible:

- **Reachability.** The simulator has no query. It has a play button on every event that could ever
  fire, and it lets you fire one that cannot (`regcf-advertising`, §3.5). It is therefore the
  over-approximation §1.1b describes, made interactive: it will show you a path to red that the
  evaluator would never take, and it cannot show you that no path reaches red.
- **Dominance.** No dominators, no "on every path from here you must do X". The `RAND` on `offering`
  shows four things live and nothing about which of them every future has to pass through.
- **And one more this run adds:** P2a **cannot test the list either**. Nothing here says whether a
  reader given `obligations`/`discharging_events`/`breaching_events` as text would have done better
  than a reader given this animation, because no reader was given either. That is P2a′, and it
  remains unrun.

---

## 6. What this means for the §7.3 gate — stated without deciding it

§7.3: _build P2d/P2e only if P2a′ shows readers cannot answer the question from the list — **and**
P2a shows the off-the-shelf simulator cannot either._

The second conjunct is what this document measured. On P1's shipped output, the off-the-shelf
simulator answers "what do I owe" with a token that does not know its modality, its bearer beyond a
lane, its deadline, its cardinality, or whether it was ever triggered; it answers "what discharges"
and "what breaches" with two buttons whose meanings invert between `MUST` and `SHANT` with no
notational difference; and it shows a breach that does not end the process. Every one of those is
a fidelity loss P1's report already names (F1, F2, F3, `P-CAST`, `P-DEADLINE`, `P-CYCLE`, and
for the fork the `P-FORK-*` family #430 replaced `P-FORK` with — its own claims, not restated
here) — the animation makes them **visible in motion**, it does not close any of them. Nothing
in this run suggests the simulator could be configured into closing them: the losses are in what
the file can carry, per §1.2's _"correct category, missing vocabulary"_, and the simulator is
faithful to the file.

What this does **not** settle, and why the gate is still open:

1. **The first conjunct is unmeasured.** P2a′ is a reader experiment against a list, and no reader
   has been shown anything. Q7 makes it the primary gate for exactly the reason §1.1a gives: the
   payload is data, and a list may suffice. If it does, this document's findings are moot in the
   direction of building a picture.
2. **The reader was a script.** The harness clicked what the tool offered; it did not measure what a
   person understands from the animation, which is the quantity §7.4's literature is about.
3. **P1's output is one encoding.** Through run 3 this item read: a hand-drawn BPMN with a
   multi-instance subProcess for the fork would animate differently, and `P-FORK` said the exporter
   did not emit it. legalese/l4-ide#430 now emits exactly that, and run 4 measured it (§3.1, §3.7):
   the box animates as a box, with a count of one, and the `n`-member story — other members still
   bound after one breaches, the group ending when the last does — is still not one this simulator
   can draw, because it has no multi-instance behaviour at all (§2). So the encoding changed, the
   picture got a shape it did not have, and the cardinality gap moved from the file to the tool.
   A BPMN with lanes per cast member would still animate differently again; that remains a P1
   question, not evidence for or against P2d.

So: the second conjunct looks satisfied on this evidence, the first is untested, and the gate is
not this document's to close. Recorded as a `RESULT` block under LTS-VISUALISER.md §7.2.

---

## 7. The empirical warrant this leans on

Copied exactly from LTS-VISUALISER.md §7.4, which corrected revision 1's two wrong DOIs; not
re-derived here:

- Maslov & Poelmans, "Facilitating the comprehension of business process models for unexperienced
  modelers using token-based animations", _Information & Management_ **61**:103967, 2024.
  DOI `10.1016/j.im.2024.103967`.
- Maslov, Poelmans, Wautelet & Gailly, "Novice modelers' subjective comprehension and interaction
  with token-animated process models", _Journal of Computer Languages_ **84**:101350, 2025.
  DOI `10.1016/j.cola.2025.101350`. _(Not CHBR — the venue was wrong too.)_

And §7.4's own reading of them, which this experiment neither confirms nor contradicts because it
measured no reader: token animation reduced extraneous cognitive load for novices and did **not**
significantly improve comprehension scores directly. It is a warrant for the audience thesis, not
for expecting readers to get more answers right — and, on the evidence above, not for expecting
this particular animation to say what the rule says.

---

## 8. Files

- `etc/bpmn-token-sim/` — `package.json`, `src/app.js`, `index.html`, `build.mjs`, `run.mjs`,
  `README.md`, `package-lock.json` (committed, so `npm ci` pins the transitive tree),
  `.gitignore` (`node_modules/`, `dist/` ignored; `out/` un-ignored).
- `etc/bpmn-token-sim/out/` — 52 screenshots (1,957,515 bytes in all, the largest 57,995;
  `find -size +200k`: none), sixteen `<fixture>.json` (121,983 bytes), `run-meta.json` (4,658) — 69
  files, every one from run 4 (2026-09-18 23:23:21 UTC, harness `9cd110c5d`, committed
  `a3c7bdce3`); `run-meta.json`'s `perFixture` block is generated and carries that one `runAt`
  sixteen times. The harness has been diffable since `983d4a82a`, and run 4 is the first `out/`
  that is: the same set run twice differs in `run-meta.json`'s `runAt` lines and nothing else
  (**Run 4** bullet at the top).
