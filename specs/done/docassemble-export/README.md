# docassemble-export

Evidence artifacts for `../DOCASSEMBLE-EXPORT-SPEC.md`.

- `probe_headless.py` — the executed R10 feasibility probe (spec Appendix B): runs a
  three-block interview through parse/assemble/answer/finish using `docassemble.base`
  1.10.7 in a plain Python 3.12 venv, no server. Recipe and gotchas in spec §8.10.
