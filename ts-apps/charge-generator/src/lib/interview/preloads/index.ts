import type { Preload } from '../preload'
import { LEWIS_CHRISTINE } from './lewis-christine'

/** One per bench case. Order = the widget's cycle order. */
export const PRELOADS: readonly Preload[] = [LEWIS_CHRISTINE]
