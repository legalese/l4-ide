{
  "note": [
    "EXPECTED TO FAIL. See the header of reconstruct-refused.dmn.",
    "",
    "`anniv_duration` is the CONTROL and passes: it is the idiom the exporter",
    "emits, and it clamps to 2025-02-28 on both engines. `anniv_reconstruct` is",
    "the idiom under test and answers null, so the run is red. Both pins name the",
    "same clamped date on purpose: the two differ only in how they were written."
  ],
  "cases": [
    {
      "name": "a 29 February issuance: reconstruction answers null, the duration clamps",
      "context": { "issued": { "$date": "2024-02-29" } },
      "expect": {
        "anniv_reconstruct": { "$date": "2025-02-28" },
        "anniv_duration": { "$date": "2025-02-28" }
      }
    }
  ]
}
