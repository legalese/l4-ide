> Module FlatTax

# FlatTax

Generated by `l4 catala` from `flat-tax.l4`.
Do not edit by hand; regenerate from the L4 source instead.

Notes from the lowering — divergences between this artifact and its L4 source
are disclosed here rather than left to be discovered (R10, R11, §8.4):

- parameter `period` of `flat tax on salary` is a STRING; Catala has no string type, so it is elided from the emitted signature (R11). Any use of its value is an error.

```catala-metadata
declaration structure Person:
  data salary content decimal

#[description = "Flat tax on salary"]
declaration scope FlatTaxOnSalary:
  input p content Person
  output flat_tax_on_salary content decimal

#[description = "Test 1, from the L4 directive at flat-tax.l4:18:1-66."]
#[test] declaration scope Test1:
  output result content decimal
```

## `flat tax on salary`

Flat tax on salary

```catala
scope FlatTaxOnSalary:
  definition flat_tax_on_salary
    equals
      (p.salary * 0.25)
```

## Tests

One scope per `#EVAL` / `#ASSERT` directive in the L4 source, with the
arguments the directive supplied. The expected values below were computed
by L4's own evaluator, so `clerk test` compares the two languages rather
than comparing Catala with itself (R7, §8.7).

```catala
scope Test1:
  definition result
    equals
      (output of FlatTaxOnSalary with { -- p: Person { -- salary: 2000.0 } }).flat_tax_on_salary
```

```catala-test-cli
$ catala test-scope Test1 --disable-warnings -F json
{"result":"500"}
```

L4 computes that as: `500`.
