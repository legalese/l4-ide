fidelity report — dmnmd
  [D-MD-NODRG] blocking — definitions_service_exhibit
      dmnmd is a table format, not a graph: the 5 information requirement(s) between these decisions have no markdown form
      lost: the decision requirements graph; which decision feeds which is invisible
  [D-MD-NOLITERAL] blocking — decision_base_charge
      `base charge` is a formula (40), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_special_case
      `special case` is a formula ((`special rate` OF 150, 5) PLUS `base charge`), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_assessment_floor
      `assessment floor` is a formula (100), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NONIDENTCOLUMN] blocking — decision_special_rate
      `p > assessment_floor` is an input EXPRESSION; a dmnmd column header is a variable name (letter, then alphanumerics/spaces/underscores), so this table is omitted
      lost: the whole table
