fidelity report — DMN 1.3 (XML), camunda flavor
  [D-VERDICT] lossy — decision_reporting_duty  (main.l4:55:5-64:111)
      `reporting duty` has a deontic body; it is lowered to a VERDICT decision table over its guards, each rule's output a string naming the arm. The obligation semantics (PARTY/MUST/WITHIN/HENCE/LEST) are the BPMN exporter's job (`l4 export --to=bpmn`): WITHIN deadlines are the exported deadline decisions, the HENCE loop is the BPMN loop-back edge, and a BPMN gateway consumes this verdict to choose the process path
      lost: the obligation as a state: PARTY/WITHIN/HENCE/LEST do not survive into this artifact; the verdict names the arm, the BPMN projection carries the lifecycle
  [D-LITERALEXPR] blocking — decision_joint_duty  (main.l4:70:5-73:19)
      `joint duty` is a deontic (regulative) body: DMN has no notion of time or obligation, so it is emitted as a boxed literal expression rather than a decision table. Its body could not be rendered as FEEL, so the emitted text is raw L4 that NO engine can evaluate
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] advisory — decision_invoked_joint_duty  (main.l4:77:36-56)
      `invoked joint duty` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table; the expression is FEEL, and an engine evaluates it
      lost: the decision-table analyses (gap, overlap, consistency): the literal computes, but it is not rules a checker can reach
  [D-LITERALEXPR] blocking — decision_bare_duty  (main.l4:81:19-65)
      `bare duty` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table. Its body could not be rendered as FEEL, so the emitted text is raw L4 that NO engine can evaluate
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] advisory — decision_invoked_bare_duty  (main.l4:84:27-38)
      `invoked bare duty` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table; the expression is FEEL, and an engine evaluates it
      lost: the decision-table analyses (gap, overlap, consistency): the literal computes, but it is not rules a checker can reach
  [D-PARAM-AS-INPUT] advisory — input_trigger
      the GIVEN parameter `trigger` of two decisions (`joint duty`, `invoked joint duty`) became the one shared model input `input_trigger`; those decisions can no longer be applied twice to different subjects within this model, and the argument expressions at their call sites are discarded
      lost: per-call-site argument binding: L4 applied these decisions to expressions; the DMN model reads one global value
  [D-PARTIAL] lossy — decision_reporting_duty  (main.l4:49:1-64:111)
      `reporting duty` could not be certified total (TERMINATES at main.l4:49:1-64:111: self-recursion could not be certified structurally terminating (no parameter position decreases through a FOLLOWED BY pattern on every recursive call)), so it is not un-lifted. Its deontic body is lowered to a verdict decision table (see D-VERDICT), which always answers one row, and no raw-L4 call site of it remains in the artifact; what could not be certified is the L4 source itself — the HENCE self-recursion may not terminate — and the artifact does not carry that recursion
      lost: the certainty that the SOURCE obligation spine terminates: the verdict table answers regardless, and the L4 recursion it summarises is what could not be certified
  [D-FLAVOR-NOSERVICE] advisory — service_deontic_verdict
      the decisionService `deontic_verdict` (§ `deontic verdict`) is emitted as GROUPING only: no call site invokes it, and on this flavor none could — the `camunda` flavor never emits the knowledgeRequirement→decisionService edge an invocation needs (§13.4/§13.5)
      lost: nothing: an uninvoked grouping service is inert on both engines (probe D1)
  [D-ORDERDEPENDENT] advisory — decision_reporting_duty  (main.l4:55:10-25)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
