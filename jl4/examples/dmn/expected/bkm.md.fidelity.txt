fidelity report — dmnmd
  [D-MD-NODRG] blocking — definitions_bkm_exhibit
      dmnmd is a table format, not a graph: the 4 information requirement(s) between these decisions have no markdown form
      lost: the decision requirements graph; which decision feeds which is invisible
  [D-MD-NOBKM] blocking — bkm_weighted_sum
      `weighted sum` is a businessKnowledgeModel — a named function of 2 parameter(s) — and dmnmd has no function form, so it is omitted, along with every invocation of it inside the decisions that remain
      lost: the function itself, and the meaning of every call site that names it: in the markdown those calls reference a definition that is not there
  [D-MD-NOBKM] blocking — bkm_boosted
      `boosted` is a businessKnowledgeModel — a named function of 1 parameter(s) — and dmnmd has no function form, so it is omitted, along with every invocation of it inside the decisions that remain
      lost: the function itself, and the meaning of every call site that names it: in the markdown those calls reference a definition that is not there
  [D-MD-NOBKM] blocking — bkm_adjusted
      `adjusted` is a businessKnowledgeModel — a named function of 2 parameter(s) — and dmnmd has no function form, so it is omitted, along with every invocation of it inside the decisions that remain
      lost: the function itself, and the meaning of every call site that names it: in the markdown those calls reference a definition that is not there
  [D-MD-NOLITERAL] blocking — decision_case_one
      `case one` is a formula (boosted(x: 10)), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_case_two
      `case two` is a formula (boosted(x: 20)), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_pay_check
      `pay check` is a formula (pay_hydrated.total + 3), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_baseline
      `baseline` is a formula (7), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_adj_one
      `adj one` is a formula (adjusted(y: 1, baseline: baseline)), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_adj_two
      `adj two` is a formula (adjusted(y: 2, baseline: baseline)), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOCONTEXT] blocking — hydration_pay
      `pay` is a boxed context: a record hydrated from its source with 3 component(s), of which the derived ones are computed from earlier siblings. dmnmd has no boxed-context form -- it can say a decision over cases and nothing else -- so this decision is omitted
      lost: the decision itself, and with it every derived component the tables downstream read through: in the markdown those reads name a decision that is not there
