fidelity report — dmnmd
  [D-MD-NODRG] blocking — definitions_deontic_verdict
      dmnmd is a table format, not a graph: the 8 information requirement(s) between these decisions have no markdown form
      lost: the decision requirements graph; which decision feeds which is invisible
  [D-MD-NOLITERAL] blocking — decision_joint_duty
      `joint duty` is a formula (IF trigger THEN ((PARTY Issuer MUST `file jointly` WITHIN 5) RAND (PARTY Issuer MUST `notify investors` WITHIN 5)) ELSE FULFILLED), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_invoked_joint_duty
      `invoked joint duty` is a formula (joint_duty), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_bare_duty
      `bare duty` is a formula (PARTY Issuer MUST `notify investors` WITHIN 10), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_invoked_bare_duty
      `invoked bare duty` is a formula (bare_duty), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
