fidelity report — DMN 1.3 (XML), camunda flavor
  [D-COMPUTEDFIELD] advisory — itemdef_pay_hydrated
      `Pay` is hydrated as `Pay_hydrated`, whose component(s) `total` are DERIVED: L4 defines each by a MEANS clause on the DECLARE, and this model computes them in the boxed context of the hydrated decision from the components declared before them. DMN cannot say so -- tItemDefinition has no derived flag, and a contextEntry is indistinguishable from a supplied value -- so a reader of the hydrated type alone cannot tell a derived component from a stored one
      lost: nothing an engine needs -- hydration means no caller ever supplies one. What is lost is a reader's ability to tell, FROM THE TYPE, which components are the model's input contract and which the model computes for itself
  [D-LITERALEXPR] blocking — bkm_weighted_sum  (main.l4:42:26-42)
      `weighted sum` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — bkm_boosted  (main.l4:46:19-44)
      `boosted` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_case_one  (main.l4:49:18-30)
      `case one` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_case_two  (main.l4:52:18-30)
      `case two` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_pay_check  (main.l4:55:19-37)
      `pay check` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_baseline  (main.l4:58:18-19)
      `baseline` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — bkm_adjusted  (main.l4:62:20-37)
      `adjusted` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_adj_one  (main.l4:65:17-29)
      `adj one` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_adj_two  (main.l4:68:17-29)
      `adj two` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-BKM] advisory — bkm_weighted_sum
      `weighted sum` is emitted as a businessKnowledgeModel (tier 2): it is applied to distinct argument expressions by `boosted`. Saturated call sites render as FEEL named-argument invocations with a knowledgeRequirement edge on each caller
      lost: the decision-as-variable reading: the callee no longer appears as a 0-ary decision variable, and is reachable only by invocation
  [D-BKM] advisory — bkm_boosted
      `boosted` is emitted as a businessKnowledgeModel (tier 2): it is applied to distinct argument expressions by `case one`, `case two`. Saturated call sites render as FEEL named-argument invocations with a knowledgeRequirement edge on each caller
      lost: the decision-as-variable reading: the callee no longer appears as a 0-ary decision variable, and is reachable only by invocation
  [D-BKM] advisory — bkm_adjusted
      `adjusted` is emitted as a businessKnowledgeModel (tier 2): it is applied to distinct argument expressions by `adj one`, `adj two`. Saturated call sites render as FEEL named-argument invocations with a knowledgeRequirement edge on each caller; 1 module-level free reference(s) of its body (`baseline`) are λ-lifted into extra formal parameters, supplied at every call site as `name: name` — a BKM body may read nothing beyond its parameters (measured on both engines, §6.2)
      lost: the decision-as-variable reading: the callee no longer appears as a 0-ary decision variable, and is reachable only by invocation
  [D-BKM-CONSUMERS] advisory — definitions
      3 decision(s) are emitted as businessKnowledgeModels. Both target flavors execute BKMs (KIE 8.44, Camunda 8; §13.3), but two known NON-target consumers cannot: @hbtgmbh/dmn-eval-js has no BKM support and falls through to the next rule on an unresolvable callee (a plausible wrong answer), and Camunda 7 answers null, silently
      lost: nothing on the target engines; portability to BKM-less consumers, which fail silently rather than loudly
  [D-FLAVOR-NOSERVICE] advisory — service_bkm_exhibit
      the decisionService `BKM_exhibit` (§ `BKM exhibit`) is emitted as GROUPING only: no call site invokes it, and on this flavor none could — the `camunda` flavor never emits the knowledgeRequirement→decisionService edge an invocation needs (§13.4/§13.5)
      lost: nothing: an uninvoked grouping service is inert on both engines (probe D1)
