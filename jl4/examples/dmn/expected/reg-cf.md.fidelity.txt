fidelity report — dmnmd
  [D-MD-NODRG] blocking — definitions_regulation_crowdfunding
      dmnmd is a table format, not a graph: the 9 information requirement(s) between these decisions have no markdown form
      lost: the decision requirements graph; which decision feeds which is invisible
  [D-MD-NODEFAULT] lossy — decision_is_accredited_investor
      dmnmd has no <defaultOutputEntry>, so `is accredited investor`'s OTHERWISE became a final catch-all row — which overlaps every other rule, so the hit policy had to go from U down to F
      lost: the order-free reading of a Unique table
  [D-MD-NOLITERAL] blocking — decision_combined_resources
      `combined resources` is a formula (annual_income + net_worth), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-NOLITERAL] blocking — decision_annual_limit_basis
      `annual limit basis` is a formula (min(annual_income, net_worth)), and dmnmd has no boxed-expression form: a named quantity defined by one expression, rather than a decision over cases, cannot be written as a table
      lost: the decision itself; it is omitted from the markdown
  [D-MD-CELLSYNTAX] blocking — decision_investor_limit
      rule decision_investor_limit_r2 has a cell dmnmd cannot read (a negation, a half-open or non-integer range, or an output with parentheses or a comma), so this table is omitted
      lost: the whole table
