# Hydration

<!-- Generated from L4. One table per decision; hit policy is the first header cell. -->

<!-- OMITTED: `applicant score` — a formula (applicant_hydrated.total_income / 1000), and dmnmd has no boxed-expression form. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `applicant band width` — a formula (applicant_hydrated.band_width), and dmnmd has no boxed-expression form. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `the leading applicant` — a formula (the_submission.primary), and dmnmd has no boxed-expression form. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `leading total` — a formula (the_leading_applicant_hydrated.total_income), and dmnmd has no boxed-expression form. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `capped bonus` — a rule of it answers `null` (the decision can REFUSE), and dmnmd's cell grammar has no null. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `bonus is settled` — a formula (b != null), and dmnmd has no boxed-expression form. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `bonus is unsettled` — a formula (c = null), and dmnmd has no boxed-expression form. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `bonus or zero` — a formula (if m != null then m else 0), and dmnmd has no boxed-expression form. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `applicant` — a boxed context (a hydrated record), which dmnmd cannot express. Located, with its code, in the fidelity report. -->

<!-- OMITTED: `the leading applicant` — a boxed context (a hydrated record), which dmnmd cannot express. Located, with its code, in the fidelity report. -->
