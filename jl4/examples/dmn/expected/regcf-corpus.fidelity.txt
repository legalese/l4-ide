fidelity report — DMN 1.3 (XML), camunda flavor
  [D-SCOPE] lossy — input_amount_to_be_sold_in_this_transaction
      two different terms fold to the FEEL name `amount_to_be_sold_in_this_transaction` (L4: `amount to be sold in this transaction`; used in aggregate amount sold to this investor including this transaction, investor is within the investment limit); they are emitted as the distinct globals `amount_to_be_sold_in_this_transaction`, `amount_to_be_sold_in_this_transaction_2`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_arrangement
      five different terms fold to the FEEL name `arrangement` (L4: `arrangement`; used in (a)(3) — conducted through a single complying intermediary, the 21-day public availability period has run, the intermediary has discharged its investor-facing duties, intermediary obligations are met, the transaction qualifies for the section 4(a)(6) exemption); they are emitted as the distinct globals `arrangement`, `arrangement_2`, `arrangement_3`, `arrangement_4`, `arrangement_5`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_filing
      two different terms fold to the FEEL name `filing` (L4: `filing`; used in disclosure requirements are met, the transaction qualifies for the section 4(a)(6) exemption); they are emitted as the distinct globals `filing`, `filing_2`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_investor
      nine different terms fold to the FEEL name `investor` (L4: `investor`; used in the applicable measure of annual income or net worth, greater of annual income or net worth, lesser of annual income or net worth, either annual income or net worth is less than the cut point, investment limit, aggregate amount sold to this investor including this transaction, the accredited-investor carve-out applies to, investor is within the investment limit, the transaction qualifies for the section 4(a)(6) exemption); they are emitted as the distinct globals `investor`, `investor_2`, `investor_3`, `investor_4`, `investor_5`, `investor_6`, `investor_7`, `investor_8`, `investor_9`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_issuer
      nine different terms fold to the FEEL name `issuer` (L4: `issuer`; used in (b)(1) — not organized under State or territorial law, (b)(2) — an Exchange Act reporting company, (b)(3) — an investment company, (b)(4) — subject to a bad-actor disqualification, (b)(5) — delinquent in ongoing annual reports, (b)(6) — no specific business plan, or a blank-check business plan, issuer is excluded by Rule 100(b), issuer is eligible, the transaction qualifies for the section 4(a)(6) exemption); they are emitted as the distinct globals `issuer`, `issuer_2`, `issuer_3`, `issuer_4`, `issuer_5`, `issuer_6`, `issuer_7`, `issuer_8`, `issuer_9`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_notice
      two different terms fold to the FEEL name `notice` (L4: `notice`; used in notice complies with Rule 204(b), advertising restriction); they are emitted as the distinct globals `notice`, `notice_2`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_offering
      six different terms fold to the FEEL name `offering` (L4: `offering`; used in aggregate offering amount, offering is within the offering limit, financial statements required, financial statement requirement is satisfied, disclosure requirements are met, the transaction qualifies for the section 4(a)(6) exemption); they are emitted as the distinct globals `offering`, `offering_2`, `offering_3`, `offering_4`, `offering_5`, `offering_6`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_status
      seven different terms fold to the FEEL name `status` (L4: `status`; used in (b)(1) — became an Exchange Act reporting company, (b)(2) — one annual report filed and fewer than 300 holders of record, (b)(3) — three annual reports filed and total assets not exceeding $10,000,000, (b)(4) — all securities repurchased, (b)(5) — liquidated or dissolved, ongoing reporting obligation may terminate, ongoing reporting obligation); they are emitted as the distinct globals `status`, `status_2`, `status_3`, `status_4`, `status_5`, `status_6`, `status_7`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-SCOPE] lossy — input_transfer
      four different terms fold to the FEEL name `transfer` (L4: `transfer`; used in transfer is within the one-year restricted period, transfer falls within an exception in Rule 501(a), transfer is permitted, resale restriction); they are emitted as the distinct globals `transfer`, `transfer_2`, `transfer_3`, `transfer_4`, because DMN's inputData is global and has no scope at all
      lost: L4's lexical scoping of GIVEN parameters: a caller must now supply one value per element where the L4 has one locally-scoped name
  [D-RENAME] lossy — input_issuer_2
      `issuer` is emitted as the FEEL name `issuer_2`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_issuer_3
      `issuer` is emitted as the FEEL name `issuer_3`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_issuer_4
      `issuer` is emitted as the FEEL name `issuer_4`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_issuer_5
      `issuer` is emitted as the FEEL name `issuer_5`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_issuer_6
      `issuer` is emitted as the FEEL name `issuer_6`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_issuer_7
      `issuer` is emitted as the FEEL name `issuer_7`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_issuer_8
      `issuer` is emitted as the FEEL name `issuer_8`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_offering_2
      `offering` is emitted as the FEEL name `offering_2`, not `offering`: six elements of the DRG's flat variable namespace in this module fold to `offering` (`offering`, `offering`, `offering`, `offering`, `offering`, `offering`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_2
      `investor` is emitted as the FEEL name `investor_2`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_3
      `investor` is emitted as the FEEL name `investor_3`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_4
      `investor` is emitted as the FEEL name `investor_4`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_5
      `investor` is emitted as the FEEL name `investor_5`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_6
      `investor` is emitted as the FEEL name `investor_6`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_7
      `investor` is emitted as the FEEL name `investor_7`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_8
      `investor` is emitted as the FEEL name `investor_8`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_amount_to_be_sold_in_this_transaction_2
      `amount to be sold in this transaction` is emitted as the FEEL name `amount_to_be_sold_in_this_transaction_2`, not `amount_to_be_sold_in_this_transaction`: two elements of the DRG's flat variable namespace in this module fold to `amount_to_be_sold_in_this_transaction` (`amount to be sold in this transaction`, `amount to be sold in this transaction`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_offering_3
      `offering` is emitted as the FEEL name `offering_3`, not `offering`: six elements of the DRG's flat variable namespace in this module fold to `offering` (`offering`, `offering`, `offering`, `offering`, `offering`, `offering`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_offering_4
      `offering` is emitted as the FEEL name `offering_4`, not `offering`: six elements of the DRG's flat variable namespace in this module fold to `offering` (`offering`, `offering`, `offering`, `offering`, `offering`, `offering`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_offering_5
      `offering` is emitted as the FEEL name `offering_5`, not `offering`: six elements of the DRG's flat variable namespace in this module fold to `offering` (`offering`, `offering`, `offering`, `offering`, `offering`, `offering`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_arrangement_2
      `arrangement` is emitted as the FEEL name `arrangement_2`, not `arrangement`: five elements of the DRG's flat variable namespace in this module fold to `arrangement` (`arrangement`, `arrangement`, `arrangement`, `arrangement`, `arrangement`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_arrangement_3
      `arrangement` is emitted as the FEEL name `arrangement_3`, not `arrangement`: five elements of the DRG's flat variable namespace in this module fold to `arrangement` (`arrangement`, `arrangement`, `arrangement`, `arrangement`, `arrangement`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_arrangement_4
      `arrangement` is emitted as the FEEL name `arrangement_4`, not `arrangement`: five elements of the DRG's flat variable namespace in this module fold to `arrangement` (`arrangement`, `arrangement`, `arrangement`, `arrangement`, `arrangement`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_notice_2
      `notice` is emitted as the FEEL name `notice_2`, not `notice`: two elements of the DRG's flat variable namespace in this module fold to `notice` (`notice`, `notice`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_status_2
      `status` is emitted as the FEEL name `status_2`, not `status`: seven elements of the DRG's flat variable namespace in this module fold to `status` (`status`, `status`, `status`, `status`, `status`, `status`, `status`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_status_3
      `status` is emitted as the FEEL name `status_3`, not `status`: seven elements of the DRG's flat variable namespace in this module fold to `status` (`status`, `status`, `status`, `status`, `status`, `status`, `status`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_status_4
      `status` is emitted as the FEEL name `status_4`, not `status`: seven elements of the DRG's flat variable namespace in this module fold to `status` (`status`, `status`, `status`, `status`, `status`, `status`, `status`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_status_5
      `status` is emitted as the FEEL name `status_5`, not `status`: seven elements of the DRG's flat variable namespace in this module fold to `status` (`status`, `status`, `status`, `status`, `status`, `status`, `status`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_status_6
      `status` is emitted as the FEEL name `status_6`, not `status`: seven elements of the DRG's flat variable namespace in this module fold to `status` (`status`, `status`, `status`, `status`, `status`, `status`, `status`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_status_7
      `status` is emitted as the FEEL name `status_7`, not `status`: seven elements of the DRG's flat variable namespace in this module fold to `status` (`status`, `status`, `status`, `status`, `status`, `status`, `status`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_transfer_2
      `transfer` is emitted as the FEEL name `transfer_2`, not `transfer`: four elements of the DRG's flat variable namespace in this module fold to `transfer` (`transfer`, `transfer`, `transfer`, `transfer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_transfer_3
      `transfer` is emitted as the FEEL name `transfer_3`, not `transfer`: four elements of the DRG's flat variable namespace in this module fold to `transfer` (`transfer`, `transfer`, `transfer`, `transfer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_transfer_4
      `transfer` is emitted as the FEEL name `transfer_4`, not `transfer`: four elements of the DRG's flat variable namespace in this module fold to `transfer` (`transfer`, `transfer`, `transfer`, `transfer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_issuer_9
      `issuer` is emitted as the FEEL name `issuer_9`, not `issuer`: nine elements of the DRG's flat variable namespace in this module fold to `issuer` (`issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`, `issuer`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_offering_6
      `offering` is emitted as the FEEL name `offering_6`, not `offering`: six elements of the DRG's flat variable namespace in this module fold to `offering` (`offering`, `offering`, `offering`, `offering`, `offering`, `offering`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_investor_9
      `investor` is emitted as the FEEL name `investor_9`, not `investor`: nine elements of the DRG's flat variable namespace in this module fold to `investor` (`investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`, `investor`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_arrangement_5
      `arrangement` is emitted as the FEEL name `arrangement_5`, not `arrangement`: five elements of the DRG's flat variable namespace in this module fold to `arrangement` (`arrangement`, `arrangement`, `arrangement`, `arrangement`, `arrangement`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-RENAME] lossy — input_filing_2
      `filing` is emitted as the FEEL name `filing_2`, not `filing`: two elements of the DRG's flat variable namespace in this module fold to `filing` (`filing`, `filing`), and the first claimant in source order keeps the base name
      lost: the resemblance between the emitted FEEL name and the L4 name it came from; the L4 name survives in @label
  [D-LITERALEXPR] blocking — decision_reg_cf_commenced  (main.l4:104:26-40)
      `Reg CF commenced` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_2017_inflation_adjustment  (main.l4:108:39-53)
      `the 2017 inflation adjustment` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_2021_amendments  (main.l4:112:29-43)
      `the 2021 amendments` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_2022_inflation_adjustment  (main.l4:116:39-53)
      `the 2022 inflation adjustment` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_rules_in_force_include  (main.l4:121:5-46)
      `the rules in force include` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_holders_of_record_threshold  (main.l4:208:37-40)
      `holders of record threshold` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_total_assets_threshold  (main.l4:213:32-40)
      `total assets threshold` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_minimum_days_publicly_available_before_any_sale  (main.l4:219:57-59)
      `minimum days publicly available before any sale` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_days_to_file_the_annual_report_after_fiscal_year_end  (main.l4:223:62-65)
      `days to file the annual report after fiscal year end` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_business_days_to_file_form_c_tr  (main.l4:227:41-42)
      `business days to file Form C-TR` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_business_days_to_file_a_progress_update  (main.l4:231:49-50)
      `business days to file a progress update` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_days_in_the_resale_restricted_period  (main.l4:238:46-49)
      `days in the resale restricted period` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_1_not_organized_under_state_or_territorial_law  (main.l4:260:9-261:141)
      `(b)(1) — not organized under State or territorial law` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_2_an_exchange_act_reporting_company  (main.l4:267:9-268:125)
      `(b)(2) — an Exchange Act reporting company` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_3_an_investment_company  (main.l4:274:9-275:134)
      `(b)(3) — an investment company` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_4_subject_to_a_bad_actor_disqualification  (main.l4:281:9-282:87)
      `(b)(4) — subject to a bad-actor disqualification` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_5_delinquent_in_ongoing_annual_reports  (main.l4:290:9-291:204)
      `(b)(5) — delinquent in ongoing annual reports` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_6_no_specific_business_plan_or_a_blank_check_business_plan  (main.l4:297:9-298:172)
      `(b)(6) — no specific business plan, or a blank-check business plan` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_issuer_is_excluded_by_rule_100_b  (main.l4:304:9-310:84)
      `issuer is excluded by Rule 100(b)` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_issuer_is_eligible  (main.l4:316:5-51)
      `issuer is eligible` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_aggregate_offering_amount  (main.l4:336:9-337:69)
      `aggregate offering amount` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_offering_is_within_the_offering_limit  (main.l4:347:9-348:93)
      `offering is within the offering limit` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_either_annual_income_or_net_worth_is_less_than_the_cut_point  (main.l4:409:8-410:76)
      `either annual income or net worth is less than the cut point` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_aggregate_amount_sold_to_this_investor_including_this_transaction  (main.l4:432:9-433:49)
      `aggregate amount sold to this investor including this transaction` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_accredited_investor_carve_out_applies_to  (main.l4:444:9-445:70)
      `the accredited-investor carve-out applies to` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_investor_is_within_the_investment_limit  (main.l4:452:9-455:44)
      `investor is within the investment limit` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_rule_date_is_inside_the_covid_19_temporary_rules_window  (main.l4:480:9-481:55)
      `the rule date is inside the COVID-19 temporary rules window` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_financial_statement_requirement_is_satisfied  (main.l4:523:9-524:74)
      `financial statement requirement is satisfied` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_disclosure_requirements_are_met  (main.l4:540:9-543:111)
      `disclosure requirements are met` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_3_conducted_through_a_single_complying_intermediary  (main.l4:560:9-564:88)
      `(a)(3) — conducted through a single complying intermediary` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_21_day_public_availability_period_has_run  (main.l4:570:9-572:67)
      `the 21-day public availability period has run` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_intermediary_has_discharged_its_investor_facing_duties  (main.l4:578:9-581:137)
      `the intermediary has discharged its investor-facing duties` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_intermediary_obligations_are_met  (main.l4:587:9-589:81)
      `intermediary obligations are met` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_notice_complies_with_rule_204_b  (main.l4:609:9-614:87)
      `notice complies with Rule 204(b)` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_definition_terms_of_the_offering  (main.l4:619:9-620:13)
      `definition — terms of the offering` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_advertising_restriction  (main.l4:640:5-642:66)
      `advertising restriction` is a deontic (regulative) body: DMN has no notion of time or obligation, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_1_became_an_exchange_act_reporting_company  (main.l4:659:9-660:115)
      `(b)(1) — became an Exchange Act reporting company` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_2_one_annual_report_filed_and_fewer_than_300_holders_of_record  (main.l4:666:9-668:77)
      `(b)(2) — one annual report filed and fewer than 300 holders of record` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_3_three_annual_reports_filed_and_total_assets_not_exceeding_10_000_000  (main.l4:674:9-676:65)
      `(b)(3) — three annual reports filed and total assets not exceeding $10,000,000` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_4_all_securities_repurchased  (main.l4:682:9-683:123)
      `(b)(4) — all securities repurchased` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_5_liquidated_or_dissolved  (main.l4:689:9-690:100)
      `(b)(5) — liquidated or dissolved` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_ongoing_reporting_obligation_may_terminate  (main.l4:696:9-701:50)
      `ongoing reporting obligation may terminate` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_ongoing_reporting_obligation  (main.l4:713:5-718:84)
      `ongoing reporting obligation` is a deontic (regulative) body: DMN has no notion of time or obligation, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_transfer_is_within_the_one_year_restricted_period  (main.l4:740:9-741:108)
      `transfer is within the one-year restricted period` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_transfer_falls_within_an_exception_in_rule_501_a  (main.l4:747:9-755:313)
      `transfer falls within an exception in Rule 501(a)` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_transfer_is_permitted  (main.l4:761:8-762:68)
      `transfer is permitted` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_resale_restriction  (main.l4:768:5-770:103)
      `resale restriction` is a deontic (regulative) body: DMN has no notion of time or obligation, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_transaction_qualifies_for_the_section_4_a_6_exemption  (main.l4:789:9-794:58)
      `the transaction qualifies for the section 4(a)(6) exemption` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_clean_issuer  (main.l4:800:24-807:168)
      `a clean issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_foreign_issuer  (main.l4:809:26-816:168)
      `a foreign issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_delinquent_issuer  (main.l4:820:29-827:168)
      `a delinquent issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_blank_check_issuer  (main.l4:829:30-836:167)
      `a blank-check issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_offering_of  (main.l4:842:66-846:111)
      `an offering of` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_investor_with  (main.l4:853:66-858:147)
      `an investor with` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_arrangement_with  (main.l4:863:63-869:135)
      `an arrangement with` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_reporting_status_with  (main.l4:875:69-881:112)
      `a reporting status with` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_transfer_at  (main.l4:886:58-891:307)
      `a transfer at` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_certified  (main.l4:958:19-115)
      `certified` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_reviewed  (main.l4:961:18-85)
      `reviewed` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_audited  (main.l4:964:17-83)
      `audited` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_conforming_notice  (main.l4:997:29-1002:167)
      `a conforming notice` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_overreaching_notice  (main.l4:1004:32-1009:166)
      `an overreaching notice` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_notice_with_no_platform_link  (main.l4:1011:40-1016:167)
      `a notice with no platform link` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_conforming_filing  (main.l4:1082:29-1085:176)
      `a conforming filing` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_base_case_qualifies  (main.l4:1092:5-1094:59)
      `the base case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_delinquent_issuer_case_qualifies  (main.l4:1099:5-1101:59)
      `the delinquent-issuer case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_over_limit_investor_case_qualifies  (main.l4:1105:5-1107:59)
      `the over-limit-investor case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_short_availability_case_qualifies  (main.l4:1111:5-1113:59)
      `the short-availability case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_under_assured_financials_case_qualifies  (main.l4:1118:5-1120:59)
      `the under-assured-financials case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_w_offering  (main.l4:1140:20-52)
      `W offering` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_w_within_the_limit_in_2016  (main.l4:1143:5-107)
      `W within the limit in 2016` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_w_within_the_limit_in_2021_january  (main.l4:1146:5-107)
      `W within the limit in 2021 January` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_w_within_the_limit_in_2021_june  (main.l4:1149:5-107)
      `W within the limit in 2021 June` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_cut_point_the_day_before_the_2017_adjustment  (main.l4:1157:5-85)
      `the cut point the day before the 2017 adjustment` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_cut_point_on_the_day_of_the_2017_adjustment  (main.l4:1160:5-85)
      `the cut point on the day of the 2017 adjustment` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_i_3_investor  (main.l4:1171:22-62)
      `I-3 investor` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_i_3_limit_under_the_2022_rules  (main.l4:1174:5-88)
      `I-3 limit under the 2022 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_i_3_limit_under_the_2023_rules  (main.l4:1177:5-88)
      `I-3 limit under the 2023 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_i_4_investor  (main.l4:1186:22-64)
      `I-4 investor` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_i_4_limit_under_the_2022_rules  (main.l4:1189:5-88)
      `I-4 limit under the 2022 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_percentage_case_under_the_2019_rules  (main.l4:1197:5-115)
      `the percentage case under the 2019 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_investor  (main.l4:1205:20-66)
      `A investor` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_is_within_the_limit_under_the_2021_rules  (main.l4:1208:5-111)
      `A is within the limit under the 2021 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_is_within_the_limit_under_the_2019_rules  (main.l4:1211:5-111)
      `A is within the limit under the 2019 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_f_offering  (main.l4:1218:20-50)
      `F offering` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_f_requirement_under_the_2019_rules  (main.l4:1221:5-99)
      `F requirement under the 2019 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_f_requirement_under_the_2023_rules  (main.l4:1224:5-99)
      `F requirement under the 2023 rules` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_small_offering_inside_the_covid_window  (main.l4:1235:5-120)
      `a small offering inside the COVID window` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_large_offering_inside_the_covid_window  (main.l4:1238:5-99)
      `a large offering inside the COVID window` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-UNDECOMPOSABLE] advisory — decision_offering_maximum_in_a_12_month_period  (main.l4:139:15-65)
      the guard ``the rules in force include` OF `the 2021 amendments`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_offering_maximum_in_a_12_month_period  (main.l4:140:15-75)
      the guard ``the rules in force include` OF `the 2017 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_offering_maximum_in_a_12_month_period  (main.l4:141:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_offering_maximum_in_a_12_month_period  (main.l4:139:15-65)
      the input expression ``the rules in force include` OF `the 2021 amendments`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_offering_maximum_in_a_12_month_period  (main.l4:140:15-75)
      the input expression ``the rules in force include` OF `the 2017 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_offering_maximum_in_a_12_month_period  (main.l4:141:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_offering_maximum_in_a_12_month_period  (main.l4:139:15-65)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_offering_maximum_in_a_12_month_period  (main.l4:142:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_income_or_net_worth_cut_point  (main.l4:151:15-75)
      the guard ``the rules in force include` OF `the 2022 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_income_or_net_worth_cut_point  (main.l4:152:15-75)
      the guard ``the rules in force include` OF `the 2017 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_income_or_net_worth_cut_point  (main.l4:153:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_income_or_net_worth_cut_point  (main.l4:151:15-75)
      the input expression ``the rules in force include` OF `the 2022 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_income_or_net_worth_cut_point  (main.l4:152:15-75)
      the input expression ``the rules in force include` OF `the 2017 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_income_or_net_worth_cut_point  (main.l4:153:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_income_or_net_worth_cut_point  (main.l4:151:15-75)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_income_or_net_worth_cut_point  (main.l4:154:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_minimum_permitted_investment  (main.l4:160:15-75)
      the guard ``the rules in force include` OF `the 2022 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_minimum_permitted_investment  (main.l4:161:15-75)
      the guard ``the rules in force include` OF `the 2017 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_minimum_permitted_investment  (main.l4:162:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_minimum_permitted_investment  (main.l4:160:15-75)
      the input expression ``the rules in force include` OF `the 2022 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_minimum_permitted_investment  (main.l4:161:15-75)
      the input expression ``the rules in force include` OF `the 2017 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_minimum_permitted_investment  (main.l4:162:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_minimum_permitted_investment  (main.l4:160:15-75)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_minimum_permitted_investment  (main.l4:163:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:170:15-75)
      the guard ``the rules in force include` OF `the 2022 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:171:15-75)
      the guard ``the rules in force include` OF `the 2017 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:172:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:170:15-75)
      the input expression ``the rules in force include` OF `the 2022 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:171:15-75)
      the input expression ``the rules in force include` OF `the 2017 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:172:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:170:15-75)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:173:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_tier_1_ceiling  (main.l4:180:15-75)
      the guard ``the rules in force include` OF `the 2022 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_tier_1_ceiling  (main.l4:181:15-75)
      the guard ``the rules in force include` OF `the 2017 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_tier_1_ceiling  (main.l4:182:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_tier_1_ceiling  (main.l4:180:15-75)
      the input expression ``the rules in force include` OF `the 2022 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_tier_1_ceiling  (main.l4:181:15-75)
      the input expression ``the rules in force include` OF `the 2017 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_tier_1_ceiling  (main.l4:182:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_tier_1_ceiling  (main.l4:180:15-75)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_tier_1_ceiling  (main.l4:183:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_tier_2_ceiling  (main.l4:190:15-75)
      the guard ``the rules in force include` OF `the 2022 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_tier_2_ceiling  (main.l4:191:15-75)
      the guard ``the rules in force include` OF `the 2017 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_tier_2_ceiling  (main.l4:192:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_tier_2_ceiling  (main.l4:190:15-75)
      the input expression ``the rules in force include` OF `the 2022 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_tier_2_ceiling  (main.l4:191:15-75)
      the input expression ``the rules in force include` OF `the 2017 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_tier_2_ceiling  (main.l4:192:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_tier_2_ceiling  (main.l4:190:15-75)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_tier_2_ceiling  (main.l4:193:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_first_time_issuer_review_ceiling  (main.l4:200:15-75)
      the guard ``the rules in force include` OF `the 2022 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_first_time_issuer_review_ceiling  (main.l4:201:15-75)
      the guard ``the rules in force include` OF `the 2017 inflation adjustment`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_first_time_issuer_review_ceiling  (main.l4:202:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_first_time_issuer_review_ceiling  (main.l4:200:15-75)
      the input expression ``the rules in force include` OF `the 2022 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_first_time_issuer_review_ceiling  (main.l4:201:15-75)
      the input expression ``the rules in force include` OF `the 2017 inflation adjustment`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_first_time_issuer_review_ceiling  (main.l4:202:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_first_time_issuer_review_ceiling  (main.l4:200:15-75)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_first_time_issuer_review_ceiling  (main.l4:203:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:383:15-65)
      the guard ``the rules in force include` OF `the 2021 amendments`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:384:15-62)
      the guard ``the rules in force include` OF `Reg CF commenced`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:383:15-65)
      the input expression ``the rules in force include` OF `the 2021 amendments`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:384:15-62)
      the input expression ``the rules in force include` OF `Reg CF commenced`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:383:15-65)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-NONFEELOUTPUT] blocking — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:383:71-119)
      the output entry ``greater of annual income or net worth` OF investor` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate it
      lost: execution: an engine fails to compile the entry -- loudly when the rule fires, but SILENTLY (a null result, reported as success) when it is the defaultOutputEntry
  [D-NONFEELOUTPUT] blocking — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:384:71-118)
      the output entry ``lesser of annual income or net worth` OF investor` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate it
      lost: execution: an engine fails to compile the entry -- loudly when the rule fires, but SILENTLY (a null result, reported as success) when it is the defaultOutputEntry
  [D-COMPUTEDOUTPUT] advisory — decision_the_applicable_measure_of_annual_income_or_net_worth  (main.l4:385:22-98)
      the output entry `no_Regulation_Crowdfunding_figure_exists_before_commencement_on_2016_05_16` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-UNDECOMPOSABLE] advisory — decision_investment_limit  (main.l4:416:10-81)
      the guard ``either annual income or net worth is less than the cut point` OF investor` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_investment_limit  (main.l4:416:10-81)
      the input expression ``either annual income or net worth is less than the cut point` OF investor` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELOUTPUT] blocking — decision_investment_limit  (main.l4:417:10-419:44)
      the output entry `IF (`minimum permitted investment` AT LEAST (0.05 TIMES (`the applicable measure of annual income or net worth` OF investor))) THEN `minimum permitted investment` ELSE (0.05 TIMES (`the applicable measure of annual income or net worth` OF investor))` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate it
      lost: execution: an engine fails to compile the entry -- loudly when the rule fires, but SILENTLY (a null result, reported as success) when it is the defaultOutputEntry
  [D-NONFEELOUTPUT] blocking — decision_investment_limit  (main.l4:420:10-422:77)
      the output entry `IF ((0.1 TIMES (`the applicable measure of annual income or net worth` OF investor)) AT MOST `maximum amount sold to any one investor in a 12-month period`) THEN (0.1 TIMES (`the applicable measure of annual income or net worth` OF investor)) ELSE `maximum amount sold to any one investor in a 12-month period`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate it
      lost: execution: an engine fails to compile the entry -- loudly when the rule fires, but SILENTLY (a null result, reported as success) when it is the defaultOutputEntry
  [D-INLINEDLOCAL] advisory — decision_investment_limit  (main.l4:416:10-81)
      the WHERE/LET local(s) `five percent of the measure`, `ten percent of the measure` were inlined into the cells that used them; DMN has no lexically scoped intermediate value
      lost: the name the drafter gave the intermediate quantity
  [D-UNDECOMPOSABLE] advisory — decision_financial_statements_required  (main.l4:502:17-503:16)
      the guard `(`aggregate offering amount` OF offering) GREATER THAN `tier 1 ceiling`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_financial_statements_required  (main.l4:491:15-51)
      the guard `(`aggregate offering amount` OF offering) AT MOST `tier 1 ceiling`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_financial_statements_required  (main.l4:492:15-51)
      the guard `(`aggregate offering amount` OF offering) AT MOST `tier 2 ceiling`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_financial_statements_required  (main.l4:499:17-71)
      the guard `(`aggregate offering amount` OF offering) AT MOST `first-time issuer review ceiling`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:502:17-503:16)
      the input expression `(`aggregate offering amount` OF offering) GREATER THAN `tier 1 ceiling`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:496:27-63)
      the input expression ``aggregate offering amount` OF offering` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:491:15-51)
      the input expression `(`aggregate offering amount` OF offering) AT MOST `tier 1 ceiling`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:492:15-51)
      the input expression `(`aggregate offering amount` OF offering) AT MOST `tier 2 ceiling`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:499:17-71)
      the input expression `(`aggregate offering amount` OF offering) AT MOST `first-time issuer review ceiling`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_financial_statements_required  (main.l4:501:17-503:43)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_financial_statements_required  (main.l4:490:77-152)
      the output entry `the_COVID_19_temporary_rules_Rule_201_z_and_bb_are_not_modelled_here` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-INLINEDLOCAL] advisory — decision_financial_statements_required  (main.l4:501:17-503:43)
      the WHERE/LET local(s) `aggregate`, `first-time issuer relief applies`, `the COVID-19 temporary relief could change this answer` were inlined into the cells that used them; DMN has no lexically scoped intermediate value
      lost: the name the drafter gave the intermediate quantity
