fidelity report — DMN 1.3 (XML)
  [D-SCOPE] lossy — input_amount_to_be_sold_in_this_transaction
      two different terms are both named `amount to be sold in this transaction` (in aggregate amount sold to this investor including this transaction, investor is within the investment limit); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_arrangement
      two different terms are both named `arrangement` (in (a)(3) — conducted through a single complying intermediary, the 21-day public availability period has run, the intermediary has discharged its investor-facing duties, intermediary obligations are met, the transaction qualifies for the section 4(a)(6) exemption); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_filing
      two different terms are both named `filing` (in disclosure requirements are met, the transaction qualifies for the section 4(a)(6) exemption); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_investor
      two different terms are both named `investor` (in greater of annual income or net worth, either annual income or net worth is less than the cut point, investment limit, aggregate amount sold to this investor including this transaction, investor is within the investment limit, the transaction qualifies for the section 4(a)(6) exemption); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_issuer
      two different terms are both named `issuer` (in (b)(1) — not organized under State or territorial law, (b)(2) — an Exchange Act reporting company, (b)(3) — an investment company, (b)(4) — subject to a bad-actor disqualification, (b)(5) — delinquent in ongoing annual reports, (b)(6) — no specific business plan, or a blank-check business plan, issuer is excluded by Rule 100(b), issuer is eligible, the transaction qualifies for the section 4(a)(6) exemption); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_notice
      two different terms are both named `notice` (in notice complies with Rule 204(b), advertising restriction); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_offering
      two different terms are both named `offering` (in aggregate offering amount, offering is within the offering limit, financial statements required, financial statement requirement is satisfied, disclosure requirements are met, the transaction qualifies for the section 4(a)(6) exemption); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_status
      two different terms are both named `status` (in (b)(1) — became an Exchange Act reporting company, (b)(2) — one annual report filed and fewer than 300 holders of record, (b)(3) — three annual reports filed and total assets not exceeding $10,000,000, (b)(4) — all securities repurchased, (b)(5) — liquidated or dissolved, ongoing reporting obligation may terminate, ongoing reporting obligation); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-SCOPE] lossy — input_transfer
      two different terms are both named `transfer` (in transfer is within the one-year restricted period, transfer falls within an exception in Rule 501(a), transfer is permitted, resale restriction); DMN's inputData is global, so the emitted model has two elements a FEEL expression cannot tell apart
      lost: L4's lexical scoping of GIVEN parameters
  [D-LITERALEXPR] blocking — decision_offering_maximum_in_a_12_month_period  (main.l4:79:47-54)
      `offering maximum in a 12-month period` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_income_or_net_worth_cut_point  (main.l4:86:39-45)
      `income or net worth cut point` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_minimum_permitted_investment  (main.l4:91:38-42)
      `minimum permitted investment` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_maximum_amount_sold_to_any_one_investor_in_a_12_month_period  (main.l4:96:70-76)
      `maximum amount sold to any one investor in a 12-month period` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_tier_1_ceiling  (main.l4:101:24-30)
      `tier 1 ceiling` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_tier_2_ceiling  (main.l4:106:24-30)
      `tier 2 ceiling` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_first_time_issuer_review_ceiling  (main.l4:111:42-49)
      `first-time issuer review ceiling` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_holders_of_record_threshold  (main.l4:116:37-40)
      `holders of record threshold` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_total_assets_threshold  (main.l4:121:32-40)
      `total assets threshold` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_minimum_days_publicly_available_before_any_sale  (main.l4:127:57-59)
      `minimum days publicly available before any sale` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_days_to_file_the_annual_report_after_fiscal_year_end  (main.l4:131:62-65)
      `days to file the annual report after fiscal year end` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_business_days_to_file_form_c_tr  (main.l4:135:41-42)
      `business days to file Form C-TR` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_business_days_to_file_a_progress_update  (main.l4:139:49-50)
      `business days to file a progress update` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_days_in_the_resale_restricted_period  (main.l4:146:46-49)
      `days in the resale restricted period` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_1_not_organized_under_state_or_territorial_law  (main.l4:168:9-169:141)
      `(b)(1) — not organized under State or territorial law` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_2_an_exchange_act_reporting_company  (main.l4:175:9-176:125)
      `(b)(2) — an Exchange Act reporting company` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_3_an_investment_company  (main.l4:182:9-183:134)
      `(b)(3) — an investment company` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_4_subject_to_a_bad_actor_disqualification  (main.l4:189:9-190:87)
      `(b)(4) — subject to a bad-actor disqualification` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_5_delinquent_in_ongoing_annual_reports  (main.l4:198:9-199:204)
      `(b)(5) — delinquent in ongoing annual reports` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_6_no_specific_business_plan_or_a_blank_check_business_plan  (main.l4:205:9-206:172)
      `(b)(6) — no specific business plan, or a blank-check business plan` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_issuer_is_excluded_by_rule_100_b  (main.l4:212:9-218:84)
      `issuer is excluded by Rule 100(b)` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_issuer_is_eligible  (main.l4:224:5-51)
      `issuer is eligible` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_aggregate_offering_amount  (main.l4:244:9-245:69)
      `aggregate offering amount` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_offering_is_within_the_offering_limit  (main.l4:255:9-256:93)
      `offering is within the offering limit` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_either_annual_income_or_net_worth_is_less_than_the_cut_point  (main.l4:303:8-304:76)
      `either annual income or net worth is less than the cut point` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_aggregate_amount_sold_to_this_investor_including_this_transaction  (main.l4:326:9-327:49)
      `aggregate amount sold to this investor including this transaction` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_investor_is_within_the_investment_limit  (main.l4:338:9-341:44)
      `investor is within the investment limit` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_financial_statement_requirement_is_satisfied  (main.l4:389:9-390:74)
      `financial statement requirement is satisfied` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_disclosure_requirements_are_met  (main.l4:406:9-409:111)
      `disclosure requirements are met` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_3_conducted_through_a_single_complying_intermediary  (main.l4:426:9-430:88)
      `(a)(3) — conducted through a single complying intermediary` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_21_day_public_availability_period_has_run  (main.l4:436:9-438:67)
      `the 21-day public availability period has run` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_intermediary_has_discharged_its_investor_facing_duties  (main.l4:444:9-447:137)
      `the intermediary has discharged its investor-facing duties` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_intermediary_obligations_are_met  (main.l4:453:9-455:81)
      `intermediary obligations are met` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_notice_complies_with_rule_204_b  (main.l4:475:9-480:87)
      `notice complies with Rule 204(b)` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_definition_terms_of_the_offering  (main.l4:485:9-486:13)
      `definition — terms of the offering` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_advertising_restriction  (main.l4:495:5-497:112)
      `advertising restriction` is a deontic (regulative) body: DMN has no notion of time or obligation, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_1_became_an_exchange_act_reporting_company  (main.l4:514:9-515:115)
      `(b)(1) — became an Exchange Act reporting company` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_2_one_annual_report_filed_and_fewer_than_300_holders_of_record  (main.l4:521:9-523:77)
      `(b)(2) — one annual report filed and fewer than 300 holders of record` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_3_three_annual_reports_filed_and_total_assets_not_exceeding_10_000_000  (main.l4:529:9-531:65)
      `(b)(3) — three annual reports filed and total assets not exceeding $10,000,000` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_4_all_securities_repurchased  (main.l4:537:9-538:123)
      `(b)(4) — all securities repurchased` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_b_5_liquidated_or_dissolved  (main.l4:544:9-545:100)
      `(b)(5) — liquidated or dissolved` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_ongoing_reporting_obligation_may_terminate  (main.l4:551:9-556:50)
      `ongoing reporting obligation may terminate` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_ongoing_reporting_obligation  (main.l4:568:5-573:84)
      `ongoing reporting obligation` is a deontic (regulative) body: DMN has no notion of time or obligation, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_transfer_is_within_the_one_year_restricted_period  (main.l4:595:9-596:108)
      `transfer is within the one-year restricted period` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_transfer_falls_within_an_exception_in_rule_501_a  (main.l4:602:9-610:313)
      `transfer falls within an exception in Rule 501(a)` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_transfer_is_permitted  (main.l4:616:8-617:68)
      `transfer is permitted` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_resale_restriction  (main.l4:623:5-625:103)
      `resale restriction` is a deontic (regulative) body: DMN has no notion of time or obligation, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_transaction_qualifies_for_the_section_4_a_6_exemption  (main.l4:644:9-649:58)
      `the transaction qualifies for the section 4(a)(6) exemption` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_clean_issuer  (main.l4:655:24-662:168)
      `a clean issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_foreign_issuer  (main.l4:664:26-671:168)
      `a foreign issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_delinquent_issuer  (main.l4:675:29-682:168)
      `a delinquent issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_blank_check_issuer  (main.l4:684:30-691:167)
      `a blank-check issuer` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_offering_of  (main.l4:697:66-701:111)
      `an offering of` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_investor_with  (main.l4:708:66-713:147)
      `an investor with` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_arrangement_with  (main.l4:718:63-724:135)
      `an arrangement with` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_reporting_status_with  (main.l4:730:69-736:112)
      `a reporting status with` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_transfer_at  (main.l4:741:58-746:307)
      `a transfer at` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_certified  (main.l4:813:19-115)
      `certified` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_reviewed  (main.l4:816:18-85)
      `reviewed` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_audited  (main.l4:819:17-83)
      `audited` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_conforming_notice  (main.l4:852:29-857:167)
      `a conforming notice` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_an_overreaching_notice  (main.l4:859:32-864:166)
      `an overreaching notice` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_notice_with_no_platform_link  (main.l4:866:40-871:167)
      `a notice with no platform link` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_a_conforming_filing  (main.l4:937:29-940:176)
      `a conforming filing` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_base_case_qualifies  (main.l4:947:5-949:59)
      `the base case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_delinquent_issuer_case_qualifies  (main.l4:954:5-956:59)
      `the delinquent-issuer case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_over_limit_investor_case_qualifies  (main.l4:960:5-962:59)
      `the over-limit-investor case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_short_availability_case_qualifies  (main.l4:966:5-968:59)
      `the short-availability case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-LITERALEXPR] blocking — decision_the_under_assured_financials_case_qualifies  (main.l4:973:5-975:59)
      `the under-assured-financials case qualifies` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-UNDECOMPOSABLE] advisory — decision_investment_limit  (main.l4:310:10-81)
      the guard ``either annual income or net worth is less than the cut point` OF investor` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_investment_limit  (main.l4:310:10-81)
      the input expression ``either annual income or net worth is less than the cut point` OF investor` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELOUTPUT] blocking — decision_investment_limit  (main.l4:311:10-313:44)
      the output entry `IF (`minimum permitted investment` AT LEAST (0.05 TIMES (`greater of annual income or net worth` OF investor))) THEN `minimum permitted investment` ELSE (0.05 TIMES (`greater of annual income or net worth` OF investor))` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate it
      lost: execution: an engine fails to compile the entry -- loudly when the rule fires, but SILENTLY (a null result, reported as success) when it is the defaultOutputEntry
  [D-NONFEELOUTPUT] blocking — decision_investment_limit  (main.l4:314:10-316:77)
      the output entry `IF ((0.1 TIMES (`greater of annual income or net worth` OF investor)) AT MOST `maximum amount sold to any one investor in a 12-month period`) THEN (0.1 TIMES (`greater of annual income or net worth` OF investor)) ELSE `maximum amount sold to any one investor in a 12-month period`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate it
      lost: execution: an engine fails to compile the entry -- loudly when the rule fires, but SILENTLY (a null result, reported as success) when it is the defaultOutputEntry
  [D-INLINEDLOCAL] advisory — decision_investment_limit  (main.l4:310:10-81)
      the WHERE/LET local(s) `five percent of the greater`, `ten percent of the greater` were inlined into the cells that used them; DMN has no lexically scoped intermediate value
      lost: the name the drafter gave the intermediate quantity
  [D-LIFTEDTHRESHOLD] advisory — decision_investment_limit  (main.l4:311:10-313:44)
      `IF (`minimum permitted investment` AT LEAST (0.05 TIMES (`greater of annual income or net worth` OF investor))) THEN `minimum permitted investment` ELSE (0.05 TIMES (`greater of annual income or net worth` OF investor))` was lifted to max(...): the threshold is now inside an output expression rather than a row of the table
      lost: a compliance reader's ability to point at the threshold as a rule
  [D-LIFTEDTHRESHOLD] advisory — decision_investment_limit  (main.l4:314:10-316:77)
      `IF ((0.1 TIMES (`greater of annual income or net worth` OF investor)) AT MOST `maximum amount sold to any one investor in a 12-month period`) THEN (0.1 TIMES (`greater of annual income or net worth` OF investor)) ELSE `maximum amount sold to any one investor in a 12-month period`` was lifted to min(...): the threshold is now inside an output expression rather than a row of the table
      lost: a compliance reader's ability to point at the threshold as a rule
  [D-UNDECOMPOSABLE] advisory — decision_financial_statements_required  (main.l4:358:10-46)
      the guard `(`aggregate offering amount` OF offering) AT MOST `tier 1 ceiling`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_financial_statements_required  (main.l4:360:15-51)
      the guard `(`aggregate offering amount` OF offering) AT MOST `tier 2 ceiling`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-UNDECOMPOSABLE] advisory — decision_financial_statements_required  (main.l4:369:17-71)
      the guard `(`aggregate offering amount` OF offering) AT MOST `first-time issuer review ceiling`` has no constant endpoint, so it became its own boolean column rather than an input expression tested by a unary test
      lost: interval gap and overlap analysis over this condition
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:358:10-46)
      the input expression `(`aggregate offering amount` OF offering) AT MOST `tier 1 ceiling`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:360:15-51)
      the input expression `(`aggregate offering amount` OF offering) AT MOST `tier 2 ceiling`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-NONFEELINPUT] blocking — decision_financial_statements_required  (main.l4:369:17-71)
      the input expression `(`aggregate offering amount` OF offering) AT MOST `first-time issuer review ceiling`` is L4 source, not FEEL: this backend has no FEEL rendering for it, so the text was emitted verbatim and NO DMN engine can evaluate this column
      lost: execution: the table is well-formed DMN but the engine fails to compile the column, and every rule that tests it is silently treated as non-matching
  [D-ORDERDEPENDENT] advisory — decision_financial_statements_required  (main.l4:358:10-46)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-INLINEDLOCAL] advisory — decision_financial_statements_required  (main.l4:358:10-46)
      the WHERE/LET local(s) `aggregate`, `first-time issuer relief applies` were inlined into the cells that used them; DMN has no lexically scoped intermediate value
      lost: the name the drafter gave the intermediate quantity
