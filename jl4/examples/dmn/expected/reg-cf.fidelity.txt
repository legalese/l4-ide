fidelity report — DMN 1.3 (XML)
  [D-LITERALEXPR] blocking — decision_combined_resources  (main.l4:66:3-35)
      `combined resources` is not a guarded chain (IF / BRANCH / CONSIDER), so it has no rows, so it is emitted as a boxed literal expression rather than a decision table
      lost: every decision-table analysis: gap, overlap, consistency and manual review
  [D-ORDERDEPENDENT] advisory — decision_financial_statements_required  (main.l4:83:8-34)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-INLINEDLOCAL] advisory — decision_financial_statements_required  (main.l4:83:8-34)
      the WHERE/LET local(s) `aggregate`, `first-time issuer relief applies` were inlined into the cells that used them; DMN has no lexically scoped intermediate value
      lost: the name the drafter gave the intermediate quantity
  [D-ORDERDEPENDENT] advisory — decision_investor_limit  (main.l4:104:8-32)
      hit policy First is order-dependent: DMN 8.2.10 says of such tables that "the table is hard to validate manually and therefore has to be used with care"
      lost: order-free reading; a rule cannot be checked without the rules above it
  [D-COMPUTEDOUTPUT] advisory — decision_investor_limit  (main.l4:106:12-85)
      the output entry `max(2500, annual limit basis)` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-COMPUTEDOUTPUT] advisory — decision_investor_limit  (main.l4:107:15-46)
      the output entry `annual limit basis * 10 / 100` is an expression, not a constant; DMN 8.2.9 permits that, but every published decision-table analysis defines an output entry as a constant
      lost: gap and overlap analysis of this table: DMN renders it, DMN's checkers do not check it
  [D-LIFTEDTHRESHOLD] advisory — decision_investor_limit  (main.l4:106:12-85)
      `IF (2500 AT LEAST `annual limit basis`) THEN 2500 ELSE `annual limit basis`` was lifted to max(...): the threshold is now inside an output expression rather than a row of the table
      lost: a compliance reader's ability to point at the threshold as a rule
