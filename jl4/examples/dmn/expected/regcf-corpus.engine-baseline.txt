# Expected external-engine verdicts for the Reg CF corpus DMN.
#
# One line per engine, matched as a FIXED SUBSTRING against that engine's stdout
# by .github/workflows/pr-checks.yml. Lines beginning with '#' are comments and
# blank lines are ignored.
#
# WHY THIS FILE EXISTS. These two strings used to be typed into `grep -q` calls
# inside the workflow, which made the DMN engine gate structurally single-
# subject: a second subject declaring a p7-dmn leg had nowhere to put its own
# verdicts, so it would have been exercised by nothing. The counts here are
# MEASURED against this corpus and belong beside it, on the same principle as
# etc/bpmn-kie-baseline.txt.
#
# These numbers are a measurement, not a target. If a change moves them, do not
# edit this file until you know WHICH decisions moved and why -- a count that
# drifts down by one is a decision that stopped being evaluated, and blessing it
# here is how that becomes the expected behaviour.
#
# 2026-09-05, ruling D1: 22 -> 23 cases, 1540 -> 1610, 330 -> 345. The new case is
# the FIRST here to ask a rule date below Reg CF's 2016-05-16 commencement, and the
# first to supply NEITHER refusal -- both are explicit JSON nulls rather than the
# -1 and the invented assurance level every other case hands the model. Until it
# existed, the floor arm ruling R2 (CORPUS-TRACK 8) put in the artifact was
# reachable by nothing, so this gate said 1540/1540 about a model whose
# pre-commencement half had never been evaluated. Sixteen of the seventy answers
# move there; seven of them move to a LAUNDERED `false` (FEEL's null reads as
# false at the first boolean consumer), which regcf-corpus.cases.json pins as
# evidence and explicitly not as endorsement.
KIE 8.44.0.Final VERDICT: 1 file(s), 23 case(s), 0 error(s), 0 warning(s), 1610/1610 decision(s) SUCCEEDED, 1610/1610 value(s) as expected, 345/345 service output value(s) as expected
Camunda 8.7.6 (zeebe-dmn) VERDICT: 1 file(s), 23 case(s), 1 parsed, 0 error(s), 1610/1610 decision(s) evaluated, 1610/1610 value(s) as expected
