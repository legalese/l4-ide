`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `miles per dollar` is equal to 1.4
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `miles per dollar` is equal to 2.4
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `miles per dollar` is equal to 1.2
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in yen` 's `miles per dollar` is equal to 2.2
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `status` is equal to `Unconfirmed`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `status` is equal to `Unconfirmed`
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `status` is equal to `Unconfirmed`
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in yen` 's `status` is equal to `Unconfirmed`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `conversion status` is equal to `Unconfirmed`
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `conversion status` is equal to `Unconfirmed`
`count` with `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `sources` is equal to 0
`count` with `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `sources` is equal to 0
`count` with `Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `sources` is equal to 0
`count` with `Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in yen` 's `sources` is equal to 0
`count` with `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `conditions` is equal to 1
`count` with `Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in yen` 's `conditions` is equal to 1
not `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `cap` 's `capped`
not `Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `cap` 's `capped`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `cap` 's `basis` is equal to `Unconfirmed basis`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `cap` 's `attribution` is equal to `Unconfirmed attribution`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `miles per dollar if the merchant flags online` is equal to `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `miles per dollar if the merchant flags card-present`
not `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `depends on the merchant indicator`
not `Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in yen` 's `depends on the merchant indicator`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `earns the bonus rate`
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in yen` 's `earns the bonus rate`
not `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `earns the bonus rate`
not `Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `earns the bonus rate`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen` 's `miles per dollar` is greater than `UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `miles per dollar`
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in yen` 's `miles per dollar` is greater than `Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars` 's `miles per dollar`
`UOB PRVI Miles earns on` with `UOB PRVI Miles`, `any cardholder`, `any month` and `a purchase in yen`
`Citi PremierMiles earns on` with `Citi PremierMiles`, `any cardholder`, `any month` and `a purchase in Singapore dollars`
