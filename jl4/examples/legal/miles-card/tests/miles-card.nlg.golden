`the rate on` with `DBS yuu Visa`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `miles per dollar`
`the rate on` with `UOB Lady's Solitaire`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `miles per dollar`
`the rate on` with `DBS Woman's World`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `depends on the merchant indicator`
`the rate on` with `HSBC Revolution`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `miles per dollar`
`the rate on` with `DBS yuu Visa`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `miles per dollar` is equal to 10.08
`the rate on` with `UOB Lady's Solitaire`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `miles per dollar` is equal to 4
`the rate on` with `DBS Woman's World`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `depends on the merchant indicator`
`the rate on` with `HSBC Revolution`, `the household`, `mid-month, yuu unlocked` and `groceries at Cold Storage by phone tap` 's `miles per dollar` is equal to 0.4
