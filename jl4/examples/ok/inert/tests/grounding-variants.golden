Parsing successful
Typechecking successful
grounding-variants.l4:25:8-42:
  I found a problem while checking the assumption of `a prohibition notice is in force`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `a prohibition notice is in force`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `a prohibition notice is in force` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:26:8-40:
  I found a problem while checking the assumption of `the visitor holds a valid pass`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `the visitor holds a valid pass`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `the visitor holds a valid pass` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:27:8-39:
  I found a problem while checking the assumption of `the site is a restricted area`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `the site is a restricted area`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `the site is a restricted area` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:29:8-30:
  I found a problem while checking the assumption of `the return was filed`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `the return was filed`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `the return was filed` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:30:8-27:
  I found a problem while checking the assumption of `the levy was paid`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `the levy was paid`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `the levy was paid` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:31:8-34:
  I found a problem while checking the assumption of `an extension was granted`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `an extension was granted`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `an extension was granted` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:33:8-38:
  I found a problem while checking the assumption of `the premises are residential`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `the premises are residential`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `the premises are residential` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:34:8-33:
  I found a problem while checking the assumption of `smoke alarms are fitted`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `smoke alarms are fitted`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `smoke alarms are fitted` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
grounding-variants.l4:35:8-35:
  I found a problem while checking the assumption of `the escape route is clear`:
    ASSUME is an older way of introducing a name, and it is being retired.
    This one leaves
    
      `the escape route is clear`
    
    open for somebody outside the file to supply. Say that with a GIVEN
    indented under the heading of the section whose rules read it:
    
      § <heading>
          GIVEN `the escape route is clear` IS A BOOLEAN
    
    If it instead marks a case the rules cannot answer, write in its place
    
      REFUSE "<the reason>"
    
    See doc/reference/types/ASSUME.md for the recipe.
