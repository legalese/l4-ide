the distinct elements of `_ UNION _` with `nyers` and `_ INTERSECT _` with `njers` and `voters`
the distinct elements of `_ UNION _` with `nyers` and `_ INTERSECT _` with `njers` and `voters`
the distinct elements of the union of `_ UNION _` with `a` and `b` and `c`
the distinct elements of the union of `_ UNION _` with `a` and `b` and `c`
the distinct elements of the elements common to `_ UNION _` with `nyers` and `njers` and `voters`
the distinct elements of the elements of `_ UNION _` with `a` and `b` not in `c`
the distinct elements of the elements of `_ UNION _` with `a` and `b` not in `c`
the distinct elements of the union of `_ UNION _` with `a` and `_ INTERSECT _` with `b` and `c` and `d`
the distinct elements of the union of `_ UNION _` with `a` and `_ INTERSECT _` with `b` and `c` and `d`
