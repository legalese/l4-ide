Parsing successful
Typechecking successful
Evaluation successful
regulative-reference-outer.l4:27:26-44:
  I found a problem while checking the definition of `top level reference`:
    In this action, `the agreed price` refers to
    
      `the agreed price` (at regulative-reference-outer.l4:23:1-19)
    
    rather than introducing a new name of its own.
    
    That is what a name in an action does: it names the thing it names. A
    placeholder that matches anything has to be a name nothing else is using.
    This note only appears when the thing named is defined outside the rule,
    because a name added there later can quietly capture a placeholder here.
regulative-reference-outer.l4:29:1-30:33:
  PARTY Buyer MUST pay `the agreed price` WITHIN 20 HENCE FULFILLED LEST BREACH
regulative-reference-outer.l4:31:1-32:35:
  FULFILLED
regulative-reference-outer.l4:38:26-44:
  I found a problem while checking the definition of `imported reference`:
    In this action, `the imported fee` refers to
    
      `the imported fee` (at regulative-reference-outer-lib.l4:3:1-19)
    
    rather than introducing a new name of its own.
    
    That is what a name in an action does: it names the thing it names. A
    placeholder that matches anything has to be a name nothing else is using.
    This note only appears when the thing named is defined outside the rule,
    because a name added there later can quietly capture a placeholder here.
regulative-reference-outer.l4:40:1-41:33:
  PARTY Buyer MUST pay `the imported fee` WITHIN 20 HENCE FULFILLED LEST BREACH
regulative-reference-outer.l4:42:1-43:35:
  FULFILLED
