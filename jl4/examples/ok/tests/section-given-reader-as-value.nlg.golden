`all bumped` where `the rate` is 10
`all bumped the long way` where `the rate` is 10
`all bumped` where `the rate` is 10 is equal to list of 10, 20 and 30
`all bumped the long way` where `the rate` is 10 is equal to list of 10, 20 and 30
