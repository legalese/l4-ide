`setSize` with `eligible`
`_ is in _` with carol and `eligible`
`violates 8th amendment`
`setSize` with the sum of `a1` and `b1`
`setSize` with the difference between `b1` and `a1`
`setSize` with the difference between `b1` and the sum of `a1` and `b1`
`setToList` with the difference between `b1` and the sum of `a1` and `b1`
the difference between 2 and 5
the sum of 5 and 2
`_ set equals _` with `setFromList` with list of 2, 1 and 1 and `setFromList` with list of 1 and 2
`_ set equals _` with `setFromList` with list of 1 and `setFromList` with list of 1 and 2
42 is equal to 42
a is equal to b
