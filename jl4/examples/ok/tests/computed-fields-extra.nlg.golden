`cfg` 's `version`
`pair` 's `total`
`pair` 's `difference`
`pair` 's `product`
`pair` 's `equal`
`pair` 's `left is bigger`
`equal pair` 's `equal`
`myOuter` 's `inner triple`
`myOuter` 's `uses inner computed`
`nums` 's `same`
`strs` 's `same`
