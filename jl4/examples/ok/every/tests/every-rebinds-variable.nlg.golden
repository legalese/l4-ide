executing contract `each tenant signs` at 0 with the following events: party `Stranger` did `Sign` with `Stranger` at 3
executing contract `each tenant signs` at 0 with the following events: party `alice` did `Sign` with `alice` at 3 party `bob` did `Sign` with `bob` at 4
executing contract `each tenant countersigns for herself` at 0 with the following events: party `alice` did `Countersign` with `alice` and `bob` at 3 party `bob` did `Countersign` with `bob` and `alice` at 4
executing contract `each tenant countersigns for herself` at 0 with the following events: party `alice` did `Countersign` with `alice` and `alice` at 3 party `bob` did `Countersign` with `bob` and `bob` at 4
