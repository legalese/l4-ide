the product of `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `points per dollar` and 1.5 is equal to 4.5
the product of `Base Points per dollar` and 0.5% is equal to 1.5%
the product of `POSB Bonus Points per dollar` and 0.5% is equal to 7.5%
the product of `Base and Bonus Points per dollar` and 0.5% is equal to 9%
`POSB PAssion earns on` with `POSB PAssion Debit`, `a Cardmember who has not linked the PAssion Card`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe today` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe charged in ringgit` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Guardian purchase made outside Singapore` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a bookshop purchase in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a foodpanda order billed as PayPal in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a foodpanda order charged directly in June 2025` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Gojek ride billed as PayPal in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Singtel Exclusive Retailers purchase in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `points per dollar` is equal to 18
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month one dollar short of the S$400 limb` and `a Cold Storage swipe in June 2025` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Food Republic meal in September 2024` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Food Republic meal in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Toast Box coffee in June 2025` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Night Safari ticket bought online in June 2025` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Night Safari ticket bought at the counter in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Singtel recurring bill payment in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Gojek ride in June 2025` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `miles per dollar if the merchant flags online` is equal to `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `miles per dollar if the merchant flags card-present`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `depends on the merchant indicator`
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `earns the bonus rate`
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `cap` 's `capped`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `cap` 's `capped`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe today` 's `earns the bonus rate`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month one dollar short of the S$400 limb` and `a Cold Storage swipe in June 2025` 's `earns the bonus rate`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `earns the bonus rate`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a bookshop purchase in June 2025` 's `earns the bonus rate`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage instalment plan in June 2025` 's `earns the bonus rate`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe today` 's `cap` 's `capped`
not `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a bookshop purchase in June 2025` 's `cap` 's `capped`
`cl.8 — the headline rate is above clause 7(a)'s base` with `Base and Bonus Points per dollar`
not `cl.8 — the headline rate is above clause 7(a)'s base` with `Base Points per dollar`
not `cl.8 — the headline rate is above clause 7(a)'s base` with 0
the product of `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `cap` 's `bonus spend per month` and 7.5% is equal to 25
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025` 's `miles per dollar` is equal to the product of 3 and `yuu miles per Point`
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `miles per dollar` is equal to the product of 18 and `yuu miles per Point`
`yuu miles per Point` is equal to 0.28
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `miles per point` is equal to 0.28
the result of dividing 8000 by 28800 is less than `yuu miles per Point`
`cl.3 — the date falls within the Promotion Period` with 1 October 2023
`cl.3 — the date falls within the Promotion Period` with 30 September 2025
not `cl.3 — the date falls within the Promotion Period` with 30 September 2023
not `cl.3 — the date falls within the Promotion Period` with 1 October 2025
`cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Retail purchase`
`cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Telephone or mail order`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Annual fee or bank charge`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Interest or finance charge`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Instalment plan transaction`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Balance or funds transfer`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Cash advance or quasi-cash`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Bill payment via internet banking`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Pre-authorisation`
not `cl.4 and cl.11 — the posting is a retail transaction that has posted` with `Refund or reversal`
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage mail order in June 2025` 's `points per dollar` is equal to 3
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage instalment plan in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage pre-authorisation in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage refund in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage annual fee in June 2025` 's `points per dollar` is equal to 0
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage instalment plan in June 2025` 's `points per dollar` is equal to 0
the number of items in `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage swipe in June 2025`'s `conditions` is equal to 8
the number of items in `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025`'s `conditions` is equal to 8
the number of items in `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a bookshop purchase in June 2025`'s `conditions` is equal to 4
the number of items in `POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage instalment plan in June 2025`'s `conditions` is equal to 3
the number of items in `POSB PAssion earns on` with `POSB PAssion Debit`, `a Cardmember who has not linked the PAssion Card`, `a month below both thresholds` and `a Cold Storage swipe in June 2025`'s `conditions` is equal to 2
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025` 's `status` is equal to `Source expired`
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a bookshop purchase in June 2025` 's `status` is equal to `Source expired`
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month meeting both thresholds` and `a Cold Storage swipe in June 2025`
`POSB PAssion earns on` with `POSB PAssion Debit`, `a linked Cardmember`, `a month below both thresholds` and `a Cold Storage refund in June 2025`
