`the table the teacher is placed in` with `Yael` is equal to `the BA table — rating code 101`
`the table the teacher is placed in` with `Dvora` is equal to `the MA table — rating code 102`
`the table the teacher is placed in` with `Noa` is equal to `the MA table — rating code 102`
`the teacher is academic` with `Yael`
`the teacher holds a doctorate` with `Noa`
not `the teacher holds a doctorate` with `Yael`
