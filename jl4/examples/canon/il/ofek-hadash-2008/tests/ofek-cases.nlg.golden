`BA cell` with 11 and 4 is equal to 9171.76
`the full-post percentage base for` with `Yael` and `a month in the 2024 school year` is equal to 10007.3
`the role supplements payable to` with `Yael` and `a month in the 2024 school year` is equal to 1000.73
`the monthly pay before the reduction for` with `Yael` and `a month in the 2024 school year` is equal to 11008.03
`the role supplements payable to` with `Yael` and `a month in the 2024 school year` is greater than `the floor under the homeroom supplement`
`the monthly pay of` with `Yael`, `a month in the first reduction period` and 0 is equal to 10903.453715
`the monthly pay of` with `Yael`, `a month in the second reduction period` and 0 is equal to 10875.93364
`the monthly pay of` with `Yael`, `a month in the 2024 school year` and 0 is equal to 11008.03
`the monthly pay of` with `Yael`, `a month in the first reduction period` and 130 is equal to 10773.453715
`the monthly pay of` with `Yael`, `a month in the second reduction period` and 130 is equal to 10875.93364
`MA cell` with 30 and 8 is equal to 15537.52
`the full-post percentage base for` with `Dvora` and `a month in the 2024 school year` is equal to 15574.89
`the monthly pay before the reduction for` with `Dvora` and `a month in the 2024 school year` is equal to 18300.49575
`the monthly pay before the reduction for` with `Dvora` and `a month after the school-role supplement begins` is equal to 18900.49575
`the monthly pay of` with `Dvora`, `a month after the school-role supplement begins` and 0 is equal to 18673.689801
`tosefet ofek 2022 from september 2023 in` with `the BA table — rating code 101` and 1 is greater than the product of 40 and `tosefet ofek 2022 from september 2023 in` with `the MA table — rating code 102` and 8
the difference between `the monthly pay before the reduction for` with `Dvora` and `a month in the 2024 school year` and `the monthly pay before the reduction for` with `Dvora` and `a month after the school-role supplement begins` is equal to 600
`MA cell` with 6 and 3 is equal to 8440.1
`the combined salary payable to` with `Noa` and `a month in the 2024 school year` is equal to 4220.05
`the tosefet ofek payable to` with `Noa` and `a month in the 2024 school year` is equal to 569.935
`the doctorate supplement payable to` with `Noa` and `a month in the 2024 school year` is equal to 211.0025
`the monthly pay before the reduction for` with `Noa` and `a month in the 2024 school year` is equal to 5000.9875
`the monthly pay before the reduction for` with `Yael on half a post` and `a month in the 2024 school year` is equal to 6004.38
`the monthly pay before the reduction for` with `Yael on half a post` and `a month in the 2024 school year` is greater than the product of 50 %  and `the monthly pay before the reduction for` with `Yael` and `a month in the 2024 school year`
the difference between the product of 50 %  and `the monthly pay before the reduction for` with `Yael` and `a month in the 2024 school year` and `the monthly pay before the reduction for` with `Yael on half a post` and `a month in the 2024 school year` is equal to the product of 50 %  and `the role supplements payable to` with `Yael` and `a month in the 2024 school year`
`the role supplements payable to` with `Yael on just under a third of a post` and `a month in the 2024 school year` is equal to 0
not `the post carries the role supplements` with 0.33
`the post carries the role supplements` with the result of dividing 1 by 3
`ROUND` with the product of `the value of an hour for` with `Yael`, `a month in the 2024 school year` and `a substitution hour` and 100 is equal to 6420
`ROUND` with the product of `the value of an hour for` with `Yael`, `a month in the 2024 school year` and `a school activity` and 100 is equal to 7062
`the value of an hour for` with `Yael`, `a month in the 2024 school year` and `a school activity` is greater than `the value of an hour for` with `Yael`, `a month in the 2024 school year` and `a substitution hour`
`ROUND` with the product of the product of the product of `the value of an hour for` with `Yael`, `a month in the 2024 school year` and `a school activity` and `the rate for a school activity` and `the school activity hours allowed in a half year` and 100 is equal to 158892
`ROUND` with the product of `the pay for the section 25 additional hour` with `the value of an hour for` with `Yael`, `a month in the 2024 school year` and `a substitution hour` and `FALSE` and 100 is equal to 1605
`ROUND` with the product of the product of `the value of an hour for` with `Yael`, `a month in the 2024 school year` and `a substitution hour` and `the rate for a substitution hour` and 100 is equal to 8025
`ROUND` with the product of `the study fund base after the reduction` with `the monthly pay before the reduction for` with `Yael` and `a month in the second reduction period` and `a month in the second reduction period` and 100 is equal to 1087593
`the reduction is ignored in computing` with `an employer contribution to a pension fund`
`the reduction is ignored in computing` with `the determining salary for a budgetary pension`
`the teacher may be promoted` with `Yael applying for rank 5`
not `the rank is rationed by quota` with `the rank sought by` with `Yael applying for rank 5`
`the teacher meets the conditions for promotion` with `Dvora applying for rank 9`
not `the quota admits the promotion` with `Dvora applying for rank 9`
not `the teacher may be promoted` with `Dvora applying for rank 9`
`the teacher has waited as long as section 38(i) allows` with `Dvora applying for rank 9`
`the teacher may be promoted` with `Dvora applying for rank 9 with room`
`the ordinary full-post week in` with `Yael` 's `stage of education` 's `frontal hours` is equal to 23
`the ordinary full-post week in` with `Dvora` 's `stage of education` 's `frontal hours` is equal to 26
`the hours in` with `the ordinary full-post week in` with `Yael` 's `stage of education` is equal to `the hours in` with `the ordinary full-post week in` with `Dvora` 's `stage of education`
`the hours in` with `the week after the age reduction for` with `Dvora`, 56 and `the ordinary full-post week in` with `a primary school` is equal to 32
`the hours in` with `the week after the age reduction for` with `Yael`, 56 and `the ordinary full-post week in` with `a junior high school` is equal to 34
