Parsing successful
Typechecking successful
Info | updateFileDiagnostics published different from new diagnostics - file diagnostics:
File: not-same-line-connective.l4
  Hidden: no
  Range: 12:18-12:27
  Source: check
  Severity: DiagnosticSeverity_Error
  Code: <none>
  Message:
    On one line, NOT reaches to the end of the line, so this reads as

      NOT (a AND b)

    If that is the meaning, write those brackets in. If only

      a

    is negated, put the brackets around the NOT and that alone:

      (NOT a) AND b

    or move the AND to a line of its own, starting in the same
    column as the NOT or further left.

File: not-same-line-connective.l4
  Hidden: no
  Range: 15:17-15:25
  Source: check
  Severity: DiagnosticSeverity_Error
  Code: <none>
  Message:
    On one line, NOT reaches to the end of the line, so this reads as

      NOT (a OR b)

    If that is the meaning, write those brackets in. If only

      a

    is negated, put the brackets around the NOT and that alone:

      (NOT a) OR b

    or move the OR to a line of its own, starting in the same
    column as the NOT or further left.

File: not-same-line-connective.l4
  Hidden: no
  Range: 18:22-18:35
  Source: check
  Severity: DiagnosticSeverity_Error
  Code: <none>
  Message:
    On one line, NOT reaches to the end of the line, so this reads as

      NOT (a IMPLIES b)

    If that is the meaning, write those brackets in. If only

      a

    is negated, put the brackets around the NOT and that alone:

      (NOT a) IMPLIES b

    or move the IMPLIES to a line of its own, starting in the same
    column as the NOT or further left.

File: not-same-line-connective.l4
  Hidden: no
  Range: 23:26-23:35
  Source: check
  Severity: DiagnosticSeverity_Error
  Code: <none>
  Message:
    On one line, NOT reaches to the end of the line, so this reads as

      NOT (b AND c)

    If that is the meaning, write those brackets in. If only

      b

    is negated, put the brackets around the NOT and that alone:

      (NOT b) AND c

    or move the AND to a line of its own, starting in the same
    column as the NOT or further left.

File: not-same-line-connective.l4
  Hidden: no
  Range: 29:3-29:12
  Source: check
  Severity: DiagnosticSeverity_Error
  Code: <none>
  Message:
    On one line, NOT reaches to the end of the line, so this reads as

      NOT ((a AND b) OR c)

    If that is the meaning, write those brackets in. If only

      a

    is negated, put the brackets around the NOT and that alone:

      ((NOT a) AND b) OR c

    or move the AND to a line of its own, starting in the same
    column as the NOT or further left.

File: not-same-line-connective.l4
  Hidden: no
  Range: 33:7-33:19
  Source: check
  Severity: DiagnosticSeverity_Error
  Code: <none>
  Message:
    On one line, NOT reaches to the end of the line, so this reads as

      NOT (TRUE AND FALSE)

    If that is the meaning, write those brackets in. If only

      TRUE

    is negated, put the brackets around the NOT and that alone:

      (NOT TRUE) AND FALSE

    or move the AND to a line of its own, starting in the same
    column as the NOT or further left.
