-- Kept on ASSUME deliberately (IMPLICIT-PROPS-DESIGN.md §11.1, R0: ASSUME is deprecated): the parse errors are anchored on the ASSUME lines themselves.
§ `Some Assumptions`

-- While there are three errors we only report one.
-- Reason being that the delayed errors are overwritten by
-- the hard error when we fail to parse any top-level declarations.
-- TODO: fix that.
ASSUME zero IS A

ASSUME one IS A

ASSUME three IS A
