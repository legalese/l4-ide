# Generated by `l4 docassemble` from assume-via-fn.l4.
# Do not edit by hand; regenerate from the L4 source instead.
# Emitted against the docassemble 1.10.7 block-key vocabulary
# (docassemble_base/docassemble/base/parse.py:1947 at commit 1b6678384).
metadata:
  title: "assume-via-fn"
---
id: q_amount
question: |
  amount
fields:
  - "amount": amount
    datatype: number
---
id: q_base_rate
question: |
  base rate
fields:
  - "base rate": base_rate
    datatype: number
---
id: c_over_threshold
reconsider: True
code: |
  # L4: `over threshold`
  over_threshold = ((amount + base_rate) > 100)
depends on:
  - amount
  - base_rate
sets:
  - over_threshold
---
id: driver_over_threshold
mandatory: True
reconsider: True
code: |
  # driver for @export `over threshold`
  if over_threshold:
      over_threshold_screen_holds
  else:
      over_threshold_screen_fails
---
id: ev_over_threshold_screen_holds
event: over_threshold_screen_holds
question: |
  over threshold: Holds
subquestion: |
  Whether the amount exceeds the adjusted threshold

  On the answers given, this decision evaluates TRUE.
---
id: ev_over_threshold_screen_fails
event: over_threshold_screen_fails
question: |
  over threshold: Fails
subquestion: |
  Whether the amount exceeds the adjusted threshold

  On the answers given, this decision evaluates FALSE.
---
id: review_main
event: review_answers
question: |
  Every question this interview can ask, and the answer it has.
review:
  - note: |
      amount: ${ showifdef('amount', '**not asked**') }
  - note: |
      base rate: ${ showifdef('base_rate', '**not asked**') }
