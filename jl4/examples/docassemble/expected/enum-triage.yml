# Generated by `l4 docassemble` from enum-triage.l4.
# Do not edit by hand; regenerate from the L4 source instead.
# Emitted against the docassemble 1.10.7 block-key vocabulary
# (docassemble_base/docassemble/base/parse.py:1947 at commit 1b6678384).
metadata:
  title: "enum-triage"
---
objects:
  - inc: DAObject
---
id: q_inc_assessed_severity
question: |
  assessed severity
fields:
  - "assessed severity": inc.assessed_severity
    datatype: radio
    choices:
      - "Critical"
      - "Reportable"
      - "Trivial"
    help: "Triage class assigned to the personal-data breach"
---
id: c_notification_deadline_in_days
code: |
  # L4: `notification deadline in days`
  if (inc.assessed_severity == 'Critical'):
      notification_deadline_in_days = 3
  elif (inc.assessed_severity == 'Reportable'):
      notification_deadline_in_days = 30
  else:
      notification_deadline_in_days = 0
depends on:
  - inc.assessed_severity
sets:
  - notification_deadline_in_days
---
id: driver_notification_deadline_in_days
mandatory: True
code: |
  # driver for @export `notification deadline in days`
  notification_deadline_in_days
  notification_deadline_in_days_screen_result
---
id: ev_notification_deadline_in_days_screen_result
event: notification_deadline_in_days_screen_result
question: |
  notification deadline in days: Result
subquestion: |
  Days within which the regulator must be notified of a personal-data breach

  On the answers given, the value of this decision is:

  **${ notification_deadline_in_days }**
