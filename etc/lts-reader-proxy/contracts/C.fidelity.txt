fidelity report — BPMN 2.0
  [F1] blocking — Task_0
      MUST, MAY and SHANT all draw as a task, so nothing here makes skipping this one a breach and skipping another one fine.
      lost: the deontic modality, which survives in the label and the <documentation> but has no notational force
  [P-DEADLINE-UNIT] advisory — Boundary_0
      Deadline ‘3’ carries no unit, because nothing in L4 fixes one — WITHIN is typed as a bare NUMBER (TypeCheck.hs) and the evaluator only ever adds it to an event timestamp. It is read here as days (P3D), following the commonest convention in the corpus, where event clocks are day counts (libraries/daydate.l4, `Day` AKA `Date to days`). A model stamping events in abstract ticks will need this edited.
      lost: certainty about the unit — if this model's clock is not days, the duration needs editing
  [F1] blocking — Task_1
      MUST, MAY and SHANT all draw as a task, so nothing here makes skipping this one a breach and skipping another one fine.
      lost: the deontic modality, which survives in the label and the <documentation> but has no notational force
  [F4] lossy — Task_1
      The PROVIDED condition ‘price AT LEAST 20’ becomes an opaque conditionExpression on the task's outgoing flow, because BPMN has no activity precondition.
      lost: whatever decision structure backed the guard — DMN would be its right home, and the DMN/BPMN link is an association, not a semantics
  [F3] blocking — Task_1
      This obligation can fail to become applicable at all, and BPMN has only one kind of ‘did not happen’.
      lost: the difference between an obligation that was never reached and one that was discharged
  [P-DEADLINE-UNIT] advisory — Boundary_1
      Deadline ‘3’ carries no unit, because nothing in L4 fixes one — WITHIN is typed as a bare NUMBER (TypeCheck.hs) and the evaluator only ever adds it to an event timestamp. It is read here as days (P3D), following the commonest convention in the corpus, where event clocks are day counts (libraries/daydate.l4, `Day` AKA `Date to days`). A model stamping events in abstract ticks will need this edited.
      lost: certainty about the unit — if this model's clock is not days, the duration needs editing
  [P-BRANCHGUARD] lossy — Split_2
      The gateway's arms are selected by ‘price EQUALS 20’, ‘NOT (price EQUALS 20)’, written into each outgoing flow as an opaque conditionExpression: BPMN has no way to say that these exhaust the cases and cannot overlap, which is what the L4 IF/ELSE chain they came from does say.
      lost: exhaustiveness and mutual exclusion as properties of the gateway — and whatever decision structure backed each condition, for which DMN, not BPMN, is the right home
  [P-NODMN] lossy — Split_2
      This gateway's guards were NOT delegated to the emitted DMN: the rule's own DECIDE is not an emitted DMN decision; and the guard's DECIDE is not an emitted DMN decision. The conditionExpressions above stay opaque L4 text.
      lost: the BPMN→DMN linkage for this gateway; the decision structure behind these guards is somewhere in the DMN artifact, but nothing in this file says where
  [F1] blocking — Task_4
      MUST, MAY and SHANT all draw as a task, so nothing here makes skipping this one a breach and skipping another one fine.
      lost: the deontic modality, which survives in the label and the <documentation> but has no notational force
  [P-DEADLINE-UNIT] advisory — Boundary_4
      Deadline ‘10’ carries no unit, because nothing in L4 fixes one — WITHIN is typed as a bare NUMBER (TypeCheck.hs) and the evaluator only ever adds it to an event timestamp. It is read here as days (P10D), following the commonest convention in the corpus, where event clocks are day counts (libraries/daydate.l4, `Day` AKA `Date to days`). A model stamping events in abstract ticks will need this edited.
      lost: certainty about the unit — if this model's clock is not days, the duration needs editing
  [F1] blocking — Task_6
      MUST, MAY and SHANT all draw as a task, so nothing here makes skipping this one a breach and skipping another one fine.
      lost: the deontic modality, which survives in the label and the <documentation> but has no notational force
  [P-DEADLINE-UNIT] advisory — Boundary_6
      Deadline ‘3’ carries no unit, because nothing in L4 fixes one — WITHIN is typed as a bare NUMBER (TypeCheck.hs) and the evaluator only ever adds it to an event timestamp. It is read here as days (P3D), following the commonest convention in the corpus, where event clocks are day counts (libraries/daydate.l4, `Day` AKA `Date to days`). A model stamping events in abstract ticks will need this edited.
      lost: certainty about the unit — if this model's clock is not days, the duration needs editing
  [F2] lossy — Process_aContract
      A lane says who performs the work, where L4's PARTY says who owes the obligation, and this diagram can only show the first (lanes here: S, B).
      lost: the bearer, as distinct from the performer — the two come apart whenever an agent acts for a principal
  [F5] advisory — Process_aContract
      BPMN has no as-of date, so any threshold or deadline that does appear in this diagram is a value someone must remember to edit when the rule changes.
      lost: asking the same question as of a different rule date, which the L4 source can still answer
