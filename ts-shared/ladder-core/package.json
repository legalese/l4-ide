{
  "name": "@repo/ladder-core",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, substrate-independent ladder-diagram layout: IRExpr -> BBE -> Scene IR (no DOM, no rendering backend). SVG lives in @repo/ladder-svg. See specs/todo/ladder-diagrams-2026/DESIGN.md.",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "git clean -fdx dist || echo 'git not available'",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "check": "tsc --noEmit",
    "demo": "tsx demo/ascii.ts",
    "test": "tsx --test test/*.test.ts"
  },
  "packageManager": "npm@11.0.0",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.22.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@repo/viz-expr": "*"
  }
}
